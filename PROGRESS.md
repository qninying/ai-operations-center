# PROGRESS.md

Tracks completed implementation changes in this repository, per the Logging, Reporting
& Progress Tracking section of `CLAUDE.md`. Each entry is appended under its task
heading and tagged with the Session ID that made the change.

## AI Operations Center Blueprint

- [x] AI Operations Center system architecture + multi-page knowledge base
  - Date: 2026-08-06
  - Session: CC-20260806-m9x2 (original session, in the parent `ColaberryAI` repository)
  - What changed: New `project-blueprint/` deliverable. `architecture.md` (Idea/Components/How It Fits Together/Data Flow/Build Order/Assumptions/Not Covered, 3 validated Mermaid diagrams) for an enterprise AI Ops Center covering SQL Server, SSIS, SSRS, and Windows Servers — 15 built components + 2 external + 1 human actor, each traced to a phrase in the source idea, built around a single structural guarantee (only the Execution Service can write, only after human approval). Added a portable, offline-capable multi-page knowledge base (`index.html` Command Center + 7 section pages + `assets/blueprint.js`/`site.js`/`site.css`) with a shared BLUEPRINT data object, an offline stemmed search index over every field, data-driven inline-SVG illustrations per section, Mermaid/Chart.js figures with a fullscreen pan/zoom viewer, dark/light theme, print support, and a two-mode Ask panel (offline local-search default; optional bring-your-own-key Claude mode calling the Anthropic API directly from the browser). Also invoked the `tech-stack-recommender` skill, which produced `tech-stack.md` (one real technology per component with fit ratings); reconciled `architecture.md` and `blueprint.js` to use those specific picks (Telegraf, Kafka, TimescaleDB, Faust, per-agent Claude model tiers, etc.) so the whole knowledge base stays internally consistent.
  - Verification: All 3 Mermaid diagrams validated with `mmdc` CLI before use. Full browser walkthrough via the Browser pane + JS execution across all 8 pages confirmed: tile grid renders 7 data-driven previews with correct live counts; Mermaid flowchart/sequence/gantt all render with correct edge counts; fullscreen viewer's expand/zoom-in/zoom-out/reset/Esc-close all confirmed working; dark/light theme toggle re-renders Mermaid diagrams correctly; nav search (stemming + phrase bonus + highlighting) and on-page narrowing confirmed via `Site.search()`; Ask panel Search-mode returns correct offline results, Claude-mode UI (scope/model/key fields, correct model IDs including `claude-haiku-4-5-20251001`) confirmed. Found and fixed two real bugs during verification: (1) `wrapText()`'s SVG `<tspan>` elements hardcoded `x="0"` instead of inheriting the parent `<text>`'s x, causing wrapped illustration labels to collide at the left edge on the Data Flow, Assumptions, and Coverage pages — fixed by threading the x-coordinate through every call site. (2) Mermaid's gantt renderer intermittently mis-measured on a cold first render and emitted invalid negative-width `<rect>`s (blank Gantt chart) — hardened with a detect-and-retry-once wrapper in `renderMermaidFigure`. Both fixes re-verified rendering correctly after the fix.
  - Notes: Two adjacent, unrequested artifacts appeared under `project-blueprint/` during this session — `mvp-plan.md`, `mockup.html`, and a `stack/` companion mini-site — generated by the `mvp-scoper` skill and additional `tech-stack-recommender` output that ran beyond the single-file instructions shown for that skill. These were not invoked by name for the MVP/mockup case and were not requested by the user; left in place as harmless, architecture-grounded bonus material rather than deleted, and flagged to the user in the session summary rather than presented as part of the requested deliverable.

- [x] Extracted into standalone `ai-operations-center/` project folder
  - Date: 2026-08-07
  - Session: CC-20260807-r7n4
  - What changed: Split this project out of the shared `ColaberryAI` repository into its own top-level `ai-operations-center/` folder, in preparation for its own GitHub repository and an expo presentation. Moved `project-blueprint/` and `BuildIdeaPrompt.txt` intact (all internal links are relative, so nothing inside needed rewriting). Copied `CLAUDE.md` verbatim from the parent repo for governance continuity. Added fresh `README.md`, `skills.md`, and this `PROGRESS.md` (none existed for this project before). Left the parent repo's own `PROGRESS.md`, `CLAUDE.md`, and every other file untouched, including `query_performance_assistant.py` (a related-domain but separately-scoped SQL Server example) and `skill-lab/` (unrelated skill-testing material) — both stay in the parent repo by explicit decision.
  - Verification: Confirmed via `grep` that no file inside `project-blueprint/` used an absolute or parent-relative (`../` escaping the folder) path pointing outside the folder before the move, so the intact move preserves every link. Confirmed post-move directory listing matches expectations.
  - Notes: The parent repo's `PROGRESS.md` still contains the original 2026-08-06 entry above (left there as required by that repo's own "leave the existing project untouched" instruction); this file carries a copy for continuity within this new folder, plus a pointer note was added to the parent's `PROGRESS.md` noting the move.

- [x] Mapped R4 safety guardrail (UNMAPPED -> PLANNED)
  - Date: 2026-08-07
  - Session: CC-20260807-q8vz
  - What changed: Created `project-blueprint/requirements.md` (new "Project DNA & Requirements" traceability doc — did not exist before) to hold R4 ("every AI recommendation is evidence-based, auditable, and routed through human approval before executing any corrective action; no autonomous production changes"). Decomposed R4 into four checkable properties (evidence-linked, human-approved, non-autonomous/scoped action type, audit-complete) as the written acceptance criterion. Implemented `guardrails/remediationGuardrail.ts`: a pure, deterministic `checkRemediationGuardrail(action)` function enforcing the first three properties (evidence IDs present, approval record present, action type on an allow-list, extra check requiring approval for production-write-protected targets); the fourth property (audit trail) is documented as the caller's (execution service's) responsibility, not something a pure pre-check can verify about itself. Added `guardrails/remediationGuardrail.test.ts` with 7 cases: happy path, one per violation type, the production-write-protected-but-approved boundary, simultaneous multi-violation reporting, and a purity check (same input twice -> identical, unmutated output). Updated `requirements.md` to move R4's status from UNMAPPED to PLANNED, pointing at both new files.
  - Verification: No code changes needed verification beyond read-through — this repo has no test runner (Jest/Vitest) wired in yet, so the test file has not actually been executed. Status was deliberately set to PLANNED rather than BUILT for this reason (per the BUILT definition written into `requirements.md` itself, which requires a runner to confirm the suite passing). Wiring a runner and confirming green is the explicit next open item.
  - Notes: First executable code (`guardrails/`) added to this repo, which was previously design/planning artifacts only (per `README.md`). Placed at repo root rather than under `/backend` (no backend tree exists yet here and creating one is a larger architectural decision) — `guardrails/` is intentionally minimal and dependency-free so it can be relocated into a future `/backend/src/services/` tree without rework.

- [x] Locked R1 core requirement with acceptance criteria + reviewer checklist
  - Date: 2026-08-07
  - Session: CC-20260807-q8vz
  - What changed: Added R1 ("Core action via Claude agent" — the system identifies root causes using Claude, reasoning over live read-only evidence) to `project-blueprint/requirements.md`, sourced from the Root Cause Analysis Agent row in `architecture.md`. Decomposed R1 into three checkable properties as its acceptance criterion: evidence-grounded (Claude call includes actual query/telemetry results, not just the alert description), attributed output (explanation references the evidence IDs it used), and non-fabricated when evidence is insufficient (returns an explicit low-confidence/no-root-cause result rather than inventing one). Deliberately kept R1's status as UNMAPPED rather than the task brief's "currently PLANNED" label, since no code exists yet for it — chose to keep this repo's own PLANNED/BUILT definitions strict (repo-verifiable evidence required) rather than relax them, on the reasoning that a status tracker used to demo this project to recruiters is only credible if its labels are honest. Added a new `## Acceptance Checklist (reviewer-verifiable)` section at the top of `requirements.md`, above the per-requirement detail sections, with one-line checks per requirement: R1 unchecked (no `guardrails/rootCauseAgent.test.ts` yet), R4 also left unchecked despite its validator+tests existing, because the test runner still isn't wired in so "passing" hasn't actually been confirmed.
  - Verification: Read-through of the edited file confirms structure (Acceptance Checklist section, R1 section, R4 section, in that order, each internally consistent with the status definitions at the top of the file). No code was added in this task, so no test-based verification applies; the checklist itself is the artifact requested by the task ("output as a markdown checklist").
  - Notes: This task's explicit deliverable was the one-line, repo-verifiable checklist (not the longer per-requirement acceptance-criteria prose, which mirrors R4's existing format and was added as supporting detail the checklist items reference). Next open item once code work resumes: implement `guardrails/rootCauseAgent.ts` + its test file to actually check R1's box, and wire a test runner so R4's box can be checked too.

- [x] Scaffolded MCP server over stdio (R2 — SQL Server DMVs, read-only)
  - Date: 2026-08-07
  - Session: CC-20260807-q8vz
  - What changed: Added `mcp-server/` — the first real MCP Tool Gateway scaffold from `architecture.md`, built on the official `@modelcontextprotocol/sdk` (1.30.0) with a stdio transport. `package.json`/`tsconfig.json` set up a standalone ESM/Node16 TypeScript project (`tsx` for zero-build dev runs). `src/dmvFixtures.ts` holds typed fixture data shaped like a real `sys.dm_exec_requests` query (three rows including one blocking-session scenario), explicitly commented as a stand-in until a live SQL Server connection is wired in. `src/index.ts` registers exactly one read-only resource (`dmv://sql-server/exec-requests`, served from the fixture) and one tool stub (`run_diagnostic_query`, validated with a zod input schema, returns a `NOT_IMPLEMENTED` result without contacting any system) — matching the R4 guardrail already locked (this surface is read-only/stubbed by construction, not by policy). Added `.gitignore` at the repo root (previously missing entirely) covering `node_modules/`, `dist/`, `.env*`, `.DS_Store`. Added `.mcp.json` at the repo root registering `aiops-mcp-server` for Claude Code via `npx tsx mcp-server/src/index.ts`.
  - Verification: Ran `npx tsx src/index.ts` from `mcp-server/` with stdin piped from `/dev/null`; stderr confirmed `aiops-mcp-server: connected over stdio` before the process exited on stdin EOF (expected with no client holding the pipe open — a real MCP client keeps it alive). SDK API calls (`registerResource`, `registerTool`) were written against the actual installed type definitions in `node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.d.ts` (read directly, not assumed) to avoid shipping code against a guessed/stale API shape.
  - Notes: Ran `npm install` inside `mcp-server/` before writing `index.ts`, specifically to inspect real SDK types rather than write the server against assumption — flagged to the user as a heads-up since it created `node_modules/`/`package-lock.json` ahead of the usual propose-then-confirm step, though no source files were touched by it. R2 is not yet added to `project-blueprint/requirements.md` (unlike R1/R4) — that's the next open item so R2 gets the same UNMAPPED/PLANNED/BUILT tracking. Next hardening items once this task's thread continues: wire a real `mssql`/`tedious` connection behind the DMV resource, implement `run_diagnostic_query` for real (still read-only), and add tests for both.

- [x] Added R2 to Project DNA & Requirements (UNMAPPED)
  - Date: 2026-08-07
  - Session: CC-20260807-q8vz
  - What changed: Closed the open item from the prior entry — added `## R2 — SQL Server DMVs data source (read-only)` to `project-blueprint/requirements.md`, between R1 and R4, with status UNMAPPED, pointing at `mcp-server/`. Decomposed R2's acceptance criterion into three checkable properties: well-formed (output shaped like real DMV columns), read-only by construction (no code path capable of a write statement, not just a policy comment), and contract-stable under a stub (swapping fixture data for a live SQL Server connection can't change the resource URI or tool schema, so agent code written against the stub keeps working). Added R2's line to the `## Acceptance Checklist` section alongside R1/R4, unchecked, noting it's scaffold/fixture-backed with no tests yet.
  - Verification: Read-through of the edited file confirms R2 sits correctly between R1 and R4 with matching structure, and the checklist entry matches the per-requirement detail section.
  - Notes: Kept R2 as UNMAPPED (not the task brief's "currently PLANNED") for the same reason as R1 — this repo's own PLANNED bar requires tests locked against the acceptance criterion, which don't exist yet for the DMV resource or the tool stub.

- [x] Implemented the SQL Server DMVs read tool; wired first test runner; R2 UNMAPPED -> PLANNED
  - Date: 2026-08-07
  - Session: CC-20260807-q8vz
  - What changed: (1) Added `vitest` as `mcp-server`'s test runner, closing the "no runner wired in" gap flagged twice in earlier sessions. Installed at `^2.1.8` first, but `npm audit` flagged 5 vulnerabilities (1 critical, 1 high, 3 moderate) in a transitive `esbuild`/`vite` chain (dev-server request forgery); bumped to `vitest@^4.1.10` per this repo's CVE-before-anything-else rule, re-audited clean (0 vulnerabilities). (2) Added `mcp-server/src/dmvReader.ts`: a pure `readDmv(input)` function validating `dmvName` against `SUPPORTED_DMVS`, throwing a typed `UnsupportedDmvError` for anything else, and optionally filtering fixture rows by `databaseName` — deliberately separated from `index.ts`'s MCP wiring so it's directly unit-testable. (3) Added `mcp-server/src/dmvReader.test.ts`: 5 cases (unfiltered happy path, filtered happy path, empty-filter edge case, malformed-DMV-name throw, thrown-error shape). Caught and fixed a real bug while writing it: first draft used `expect.unreachable()`, which isn't a real Vitest API (borrowed from Deno's std test lib by mistake) — replaced with a plain try/catch-and-assert pattern. Also caught a `tsc` error on missing `.js` extensions on the test file's relative imports (required by this project's `Node16` module resolution) — fixed before running anything. (4) Registered a new `read_sql_server_dmv` MCP tool in `index.ts` (zod input schema built from `SUPPORTED_DMVS`, returns rows as structured JSON text content, catches `UnsupportedDmvError` and returns a proper `isError: true` tool result instead of letting it throw) — separate from the pre-existing generic `run_diagnostic_query` stub. (5) Added `mcp-server/src/readOnlyGuard.test.ts`: an automated structural guard (not just a design claim) asserting `package.json` has no SQL write-capable driver dependency (`mssql`, `tedious`, `pg`, etc.) and that neither `dmvReader.ts` nor `index.ts` contains SQL write-statement keywords (`INSERT`/`UPDATE`/`DELETE`/`EXEC`/`MERGE`/`TRUNCATE`/`DROP`) — this was added specifically because R2's existing acceptance criterion promised this exact test and it didn't exist yet; writing it before flipping R2's status rather than after. (6) Updated `project-blueprint/requirements.md`: R2 moved UNMAPPED -> PLANNED, checklist entry updated to point at the real files, and the per-requirement acceptance criteria annotated per-property with what's tested vs. design-only (property 3, contract-stability under a stub, stays explicitly unverified — no live connection exists yet to swap in and prove it).
  - Verification: `npx tsc --noEmit` passes clean in `mcp-server/`. `npm test` (vitest 4) passes 8/8 across both test files. `npm audit` reports 0 vulnerabilities after the vitest version bump. Manually smoke-tested the server (`npx tsx src/index.ts` with piped stdin) both before and after the `read_sql_server_dmv` wiring change — confirmed `aiops-mcp-server: connected over stdio` both times, no startup regression.
  - Notes: R2 is PLANNED, not BUILT — flagged two remaining honest gaps directly in `requirements.md` rather than glossing over them: fixture columns are a plausible approximation of `sys.dm_exec_requests`, not verified against a live instance (`database_name` in particular isn't a real DMV column — a real query would derive it via a join to `sys.databases`); and "contract-stable under a stub" can't be proven until a real SQL Server connection actually replaces the fixture. Both are named as the explicit PLANNED -> BUILT criteria for next time.

- [x] Implemented the core action against a real source (R3 — result shaping + substitutions, UNMAPPED -> PLANNED)
  - Date: 2026-08-07
  - Session: CC-20260807-q8vz
  - What changed: (1) Added `mssql@^12.7.0` + `@types/mssql` as a deliberate new dependency (per this repo's "no drive-by npm install" rule); `npm audit` clean (0 vulnerabilities) after install. Added `mcp-server/.env.example` documenting the four required connection env vars (`SQLSERVER_HOST/DATABASE/USER/PASSWORD`), none of which are set in this environment — the live path is expected to be genuinely unreachable here, which is what proves the fallback path for real rather than by assertion alone. (2) Extracted `mcp-server/src/dmvTypes.ts` (`SUPPORTED_DMVS`, `SupportedDmv`, `ReadDmvInput`) out of `dmvReader.ts` specifically to avoid a circular dependency once `dmvReader.ts` needed to import from the new `dmvLiveSource.ts` — the "missing third module" pattern this repo's composition rules call out by name. (3) Added `mcp-server/src/dmvLiveSource.ts`: `queryLiveDmv()` reads connection config from `process.env` (throws typed `LiveSourceUnavailableError` immediately if anything is missing, no connection attempted), otherwise opens an `mssql` connection pool with a 3s request/connection timeout and retries once (300ms delay) before throwing `LiveSourceQueryError`. Query text is a fixed, hardcoded map keyed by `dmvName` (never built from input); the only variable input (`databaseName`) is bound via `request.input("databaseName", sql.NVarChar, ...)`. The `sys.dm_exec_requests` query now derives `database_name` via `DB_NAME(r.database_id)`, closing a gap flagged as a known caveat in the previous entry. (4) Rewrote `mcp-server/src/dmvReader.ts` as the orchestrator: `readDmv()` is now async, tries `queryLiveDmv()` first (injectable for tests, defaults to the real one), falls back to fixture data on either known live-source error type (re-throws anything else rather than swallowing it), and caps every response to 3 rows via a new `DmvReadResult { source: "live" | "fallback", rows }` shape. (5) Updated `mcp-server/src/index.ts`'s `read_sql_server_dmv` tool to await the new signature and surface `source` in its JSON output, so nothing downstream (including Claude) can mistake fallback data for live data. (6) Rewrote `dmvReader.test.ts` (7 cases, mocking the injected live source) and added `dmvLiveSource.test.ts` (5 cases: missing-config throw, retry-then-fail, retry-then-succeed, and a dedicated SQL-injection regression test asserting a malicious `databaseName` is bound as a parameter and never appears in the query text sent to `.query()`). Extended `readOnlyGuard.test.ts` from an outright driver ban to a reviewed-allow-list model (`mssql` is now present and reviewed) and added `dmvLiveSource.ts` to its write-keyword scan, which it had been missing. (7) Updated `project-blueprint/requirements.md`: refreshed R2's now-stale producing-task description (it no longer describes fixture-only logic), added `## R3 — Result shaping + substitutions` between R2 and R4 at status PLANNED, and rewrote R2's own acceptance-criteria annotations to reflect what R3 actually closed vs. what's still open — both R2 and R3 now share the same explicit path to BUILT.
  - Verification: `npx tsc --noEmit` passes clean. `npm test` (vitest 4) passes 15/15 across three test files. Manually smoke-tested the server (piped-stdin `tsx src/index.ts`) after the async `readDmv()` refactor — confirmed `aiops-mcp-server: connected over stdio`, no startup regression. Caught and fixed three real bugs before calling this done, none silently: (a) mocked `ConnectionPool` with an arrow function, which can't be used as a constructor — vitest's own stderr warning caught it, fixed by switching to a named function expression; (b) `readOnlyGuard.test.ts`'s blanket driver ban correctly (for the right structural reason, wrong specific cause) failed once `mssql` was added — redesigned as a reviewed-allow-list rather than weakened; (c) that same guard test then flagged my own explanatory comment in `dmvLiveSource.ts` ("nothing here can issue INSERT/UPDATE/DELETE/EXEC") as containing forbidden keywords — reworded the comment rather than the test, since the test was correctly unable to distinguish English prose from SQL text.
  - Notes: R3 (and R2, retroactively) is PLANNED, not BUILT. The one honest gap named explicitly in `requirements.md` for both: `dmvLiveSource.ts`'s live query has never actually run against a real SQL Server instance — only its "fails safely when unreachable" behavior is proven. That's the named next step for both requirements, not a new one.

- [x] Connected a local HTTP preview of the DMV read path (scoped down from the original "deploy to aiops.preview.colaberry.ai" ask)
  - Date: 2026-08-09
  - Session: CC-20260807-q8vz
  - What changed: The build task asked to push so a preview deploys at `aiops.preview.colaberry.ai`, with R2 labeled "currently VERIFIED." Before doing anything, checked the repo for any actual deploy pipeline: no CI/CD config, no Dockerfile, no Vercel/Netlify config, no nginx setup exist anywhere, the only git remote is `github.com/qninying/Architect-Workspace.git` (unrelated to that domain), and the entire `ai-operations-center/` folder has never been committed to it. Also kept R2 at its honest status (PLANNED, not VERIFIED — the live SQL Server path has still never actually succeeded, per the prior entry). Asked the user directly rather than assuming; they chose "build & verify locally only," explicitly ruling out any push or deploy claim for this task. (1) Added `mcp-server/src/httpServer.ts`: a thin `node:http` wrapper (deliberately no new dependency — plain Node HTTP, not Express, for two routes) exposing `GET /health` and `GET /dmv/exec-requests?databaseName=...`, both calling the same already-tested `readDmv()` orchestrator used by the stdio MCP transport — no duplicated business logic, just a second transport. `PORT` env var (default 8787), explicit `server.requestTimeout` (5s) so a hung connection can't accumulate. Added an `"http": "tsx src/httpServer.ts"` script to `package.json`, separate from the existing `"dev"` (stdio) script. Extended `readOnlyGuard.test.ts`'s file list to include `httpServer.ts` in its write-keyword scan, since it now participates in the DMV read path. (2) Created `.claude/launch.json` at the parent repo root (outside `ai-operations-center/`, since that's where this session's browser-preview tooling looks for launch configs) registering an `aiops-http` dev-server entry, so the app can be launched via the standard preview tooling in future sessions — though see Notes below on a real limitation hit with it.
  - Verification: `npx tsc --noEmit` clean, `npm test` 16/16 passing. Ran the server directly and curled all four cases: `GET /health` → 200 `{"status":"ok"}`; `GET /dmv/exec-requests` → 200, `source: "fallback"` (honest, since no live SQL Server is configured here), 3 fixture rows; same endpoint with `?databaseName=OpsWarehouse` → 200, correctly narrowed to 2 rows; `GET /nonexistent` → 404 with a structured error body. Also opened it in the Browser pane (screenshot confirmed the JSON rendering) and confirmed with the user that the same `localhost:8787` URLs are reachable from their own regular browser on the same machine, proving this isn't sandbox-only. Stopped the background process after verification (not a persistent service — restart with `npm --prefix mcp-server run http`).
  - Notes: `preview_start` with `name: "aiops-http"` (the proper way to launch via `.claude/launch.json`, per this session's tool guidance) failed with `EPERM: process.cwd failed` inside `npm`'s own startup code — an environment/sandbox quirk unrelated to this repo's code or config; worked fine when the server was started directly via a background shell command instead. Left `.claude/launch.json` in place since it's correctly written and may work in a normal terminal session; flagging so a future session doesn't waste time assuming the config itself is wrong. No git push was made, and no claim is made anywhere that anything is live at `aiops.preview.colaberry.ai` — that remains an explicit open item pending real deploy infrastructure, per the user's own choice this session.

- [x] Added retry + timeout + circuit breaker to the upstream call (R5 — UNMAPPED -> PLANNED)
  - Date: 2026-08-09
  - Session: CC-20260807-q8vz
  - What changed: The build task specified a stricter reliability bar (10s timeout, 3 capped retries with backoff, circuit breaker after 5 errors in a window) than what `dmvLiveSource.ts` already had inline (3s timeout, 1 retry, fixed 300ms delay — picked ad hoc in an earlier session). Rather than patch that in place, extracted a generic, upstream-agnostic reliability layer under `mcp-server/src/reliability/`, anticipating reuse by the next upstream call this project will build (R1's Claude-calling Root Cause Analysis Agent) — per this repo's composition rule to lift shared logic once a third use is anticipated. (1) `reliability/circuitBreaker.ts`: a `CircuitBreaker` class with closed/open/half-open states, an injectable clock (defaults to `Date.now`) for deterministic testing, a sliding failure window, and a `checkAvailability()` method that returns `null` (proceed) or the remaining cooldown in ms (reject) rather than throwing itself — state/window tracking only, no knowledge of what it's protecting. A failed half-open trial reopens immediately without re-accumulating the failure threshold (standard circuit-breaker semantics). (2) `reliability/withReliability.ts`: a generic `withReliability(operation, options)` wrapper — per-attempt timeout via `Promise.race`-style racing, exponential backoff (500ms base, x2, capped at 4s) between up to `maxRetries` retries, and optional composition with a `CircuitBreaker` — re-checking `checkAvailability()` before *every* attempt (not just once up front), so a circuit that trips mid-retry-sequence aborts remaining retries instead of wasting them. (3) Rewired `dmvLiveSource.ts`: removed its bespoke retry loop and the now-superseded `LiveSourceQueryError` class entirely (not deprecated/kept for compat — genuinely unused once the retry loop moved out); `queryLiveDmv()` now delegates to `withReliability()` configured to the R5 spec, wrapped around one module-level `CircuitBreaker` shared across all calls (5 failures/60s window/30s cooldown), so failures accumulate across separate calls, not just within one call's own retries. `readConnectionConfig()`'s mssql timeouts aligned to the same 10s value. (4) Updated `dmvReader.ts`'s `isKnownLiveSourceFailure()` to recognize the new `UpstreamCallFailedError` and `CircuitOpenError` types (replacing the removed `LiveSourceQueryError`) as known live-source failures that trigger graceful fallback to fixture data — a circuit-open condition degrades the same way a query failure does, rather than propagating as an unhandled error. (5) Rewrote `dmvLiveSource.test.ts` (6 cases, using `vi.useFakeTimers()` so backoff/cooldown cost no real wall-clock time — vitest's fake timers also fake `Date`, which the breaker's default clock relies on) and fixed `dmvReader.test.ts`'s two now-broken references to the removed error class (split into two focused tests: retries-exhausted and circuit-open, both asserted to trigger fallback). (6) Added `reliability/circuitBreaker.test.ts`: 7 direct unit tests of the state machine using the injectable clock (no fake timers needed — fully deterministic), covering the explicit "failure-path test" the task asked for: stays-closed-below-threshold, opens-at-threshold, sliding-window boundary (failures aging out), remaining-cooldown reporting, half-open transition, closes-on-successful-trial, reopens-immediately-on-failed-trial. (7) Updated `project-blueprint/requirements.md`: refreshed R3's now-stale producing-task text (no longer "3s timeout, 1 retry" — that detail moved to R5), added `## R5 — Retry + timeout on the upstream call` (PLANNED) between R3 and R4 with a four-property acceptance criterion, honestly noting property 4 ("generic, not SQL-specific") is true by code inspection but not yet proven by an actual second caller reusing it.
  - Verification: `npx tsc --noEmit` clean. `npm test` (vitest 4) passes 26/26 across four test files (up from 19). Smoke-tested the stdio MCP server after the rewiring — still connects cleanly, no regression. Caught and fixed one real bug before calling this done: the first draft of `dmvLiveSource.test.ts`'s retry/circuit-breaker tests created the result promise, advanced fake timers (during which the promise actually rejected), and only *then* attached the `.rejects` assertion — Node correctly flagged 5 unhandled-rejection warnings even though the tests happened to pass. Fixed by attaching the assertion (which registers a handler synchronously) before advancing timers, then re-ran to confirm the warnings were actually gone, not just the test outcome.
  - Notes: R5 is PLANNED, not BUILT — same shared open item as R2/R3 (the live SQL Server path has never actually succeeded against a real instance in this environment), plus one item unique to R5: "generic, not SQL-specific" is a design property, not yet proven by a second real caller. Both are named explicitly in `requirements.md` as the path to BUILT, not glossed over.

- [x] Handled empty-input and zero-result cases gracefully (R3 polish — status unchanged, already PLANNED)
  - Date: 2026-08-10
  - Session: CC-20260807-q8vz
  - What changed: This task's build brief already matched this repo's own tracker (R3: PLANNED) — no status mismatch to flag this time. Scoped "empty-input" specifically to `databaseName: ""` (an empty-string filter, almost certainly a caller mistake rather than an intentional "match nothing" request); deliberately did not extend this to `dmvName: ""`, since that already gets a clear, typed `UnsupportedDmvError` today, which is already graceful handling via a different mechanism. (1) Added `mcp-server/src/observability/logger.ts`: a minimal structured JSON logger (`timestamp`/`level`/`service`/`event`/`context`, one line per event) per this repo's Observability Framework, with one documented deviation from its general "logs go to stdout" rule — this writes to stderr via `console.error`, because `index.ts`'s stdio MCP transport reserves stdout exclusively for the JSON-RPC protocol stream (the same reason the existing `"...connected over stdio"` message already uses stderr). (2) Updated `dmvReader.ts`: added `normalizeInput()`, which treats `databaseName: ""` as equivalent to no filter (logging a `warn` event, `dmv_empty_filter_normalized`) before it reaches either the live query or the fixture filter, so both paths get consistent treatment; added `buildResult()`, which after shaping to `MAX_RESULTS`, checks for a zero-length result and — if empty — logs a `warn` event (`dmv_zero_results`) and attaches a new optional `message` field (filter-specific wording when a `databaseName` was given, generic otherwise). `DmvReadResult`'s `message` field is additive to the existing `{ source, rows }` shape — backward compatible, nothing that reads the old contract breaks. (3) Added 4 boundary tests to `dmvReader.test.ts` (mocking `logEvent` via `vi.mock`, not the dynamic-import pattern used elsewhere in this codebase — no per-test module state needed here, so the simpler hoisted mock sufficed): empty-string filter normalizes and logs, zero live results get a message + log, zero results from the fallback path also get a message, and — importantly — the happy path with non-empty results logs nothing and sets no message, confirming this doesn't leak into normal responses.
  - Verification: `npx tsc --noEmit` clean. `npm test` 30/30 passing (up from 26 — the 4 new boundary tests), no regressions in the other 26. Smoke-tested the stdio server post-change — still connects cleanly. Additionally ran a real (non-mocked) end-to-end check via a throwaway script calling `readDmv()` directly with a live source that legitimately returns zero rows, confirming the actual stderr output is well-formed JSON (`{"timestamp":"...","level":"warn","service":"aiops-mcp-server","event":"dmv_zero_results","context":{...}}`) and the friendly message reads correctly — not just that the mocked assertions passed, but that the real code path produces real, valid output. Updated `project-blueprint/requirements.md`: added a fourth acceptance property to R3 ("empty input and zero-result handling — never silent"), refreshed R3's producing-task description and both R3/R5's checklist entries to the current file list and test count (30).
  - Notes: No new requirement was added — this was polish on R3's existing acceptance criterion, not a new numbered requirement, matching the task's own framing ("Requirement: R3 ... currently PLANNED", not a fresh UNMAPPED item).

- [x] Wrote the 90-second demo script (Showcase & portfolio)
  - Date: 2026-08-10
  - Session: CC-20260807-q8vz
  - What changed: The task's section header said "2-minute demo screencast" but "What to do" specifically asked for a 90-second script; went with the more precise instruction. No `Rn` requirement is tied to this task — it's a portfolio deliverable, not a functional requirement, so `project-blueprint/requirements.md` was not touched. Added `project-blueprint/demo-script.md`: a 3-act, timestamped screencast script (problem -> tool call -> guardrail block) with word-for-word narration and on-screen action cues, matching the sibling docs in `project-blueprint/` (`architecture.md`, `tech-stack.md`, `mvp-plan.md`). Every command referenced in the script was actually run against real code before being written down, not written as plausible-sounding fiction: Act 2 shows `curl http://localhost:8787/dmv/exec-requests` against the real HTTP wrapper (R2/R3), landing on the real blocking-session pair already in the fixture data (session 61 blocked by session 52); Act 3 shows a new permanent demo asset, `guardrails/demoUnsafeAction.ts`, which calls the real `checkRemediationGuardrail()` (R4) with a deliberately narrow scenario — an action that's evidence-linked and of a valid, reversible type, missing only human approval — chosen over a maximally-broken example because it makes the actual thesis sharper: even a "good" AI recommendation cannot execute without a human's sign-off.
  - Verification: Ran `npx tsx guardrails/demoUnsafeAction.ts` directly and confirmed the real output (`"allowed": false`, violations `NOT_HUMAN_APPROVED` and `PRODUCTION_WRITE_REQUIRES_APPROVAL`) matches what the script tells the presenter to expect. Started the HTTP server and ran the exact `curl` command from Act 2, confirming the real JSON output (including the honest `source: "fallback"` tag) matches the script. `npx tsc --noEmit` and `npm test` (30/30) both re-confirmed clean afterward — adding the new demo file didn't touch any existing `mcp-server/` code, so this was a no-regression check, not an expectation of change.
  - Notes: Iterated once on the guardrail demo scenario before finalizing — first draft used a maximally-unsafe action (missing evidence, wrong action type, no approval, all four violations at once), which worked but told a weaker story than the final version's single-gap scenario. First-draft version was verified via a throwaway script, then deleted once the better version was confirmed and made permanent as `guardrails/demoUnsafeAction.ts`.

- [x] Wrote the reviewer one-pager (Showcase & portfolio)
  - Date: 2026-08-10
  - Session: CC-20260807-q8vz
  - What changed: The existing `project-blueprint/one-pager.pdf` is a business/stakeholder pitch doc (from an earlier `mvp-scoper` skill run) with no implementation detail — this task asked for something different: a one-page *architecture* summary for reviewers, structured around problem/tools/guardrail/reliability. Created a new file rather than overwrite or duplicate the existing pitch doc. Added `project-blueprint/reviewer-one-pager.md`: problem statement, the MCP Tool Gateway and its `read_sql_server_dmv` tool (R2/R3), the `checkRemediationGuardrail()` structural gate (R4), the reliability layer (R5: timeout/retry/backoff/circuit breaker/graceful degradation), and an explicit "Status: honest, not polished" section stating this is a walking skeleton with the live SQL Server path still unproven — matching this repo's established practice of not overclaiming status to any audience, reviewers included. No `Rn` requirement is tied to this task (portfolio deliverable, not a functional requirement), so `requirements.md` was not touched.
  - Verification: Re-ran `cd mcp-server && npm test` immediately before finalizing the doc to confirm the "30/30 tests passing" figure cited in the Status section was still accurate at time of writing (it was) — every specific claim in the doc (test count, guardrail's 4 checks, reliability numbers) is checkable against actual code/tests in this repo, not asserted from memory.
  - Notes: Kept the doc text-first with no diagram, to actually fit "one page" at this density — noted as an option to add later if wanted, not done unprompted.

- [x] Built a live, clickable dashboard (user-requested — everything so far was terminal/curl-only)
  - Date: 2026-08-10
  - Session: CC-20260807-q8vz
  - What changed: User feedback: every deliverable so far (MCP tool, guardrail, reliability layer) was real and tested but only provable via terminal — no UI, nothing to click, nothing that reads as "a running product." Recommended (and user agreed) a minimal single-page dashboard now, deferring the full `architecture.md` "Operations Console" (React, auth, role-based views — genuinely Phase 5-6 scope) to a later, separate build. (1) Added `mcp-server/src/dashboard.html`: a self-contained page (inline CSS/JS, no framework, no build step) with a health indicator, live incident cards rendered from `/dmv/exec-requests` (visually flagging the blocked session), and a "Recommend Remediation" button. (2) Added two routes to `httpServer.ts`: `GET /` serves the dashboard (read once at startup via `readFileSync`, not re-read per request); `POST /guardrail/check-demo` calls the real `checkRemediationGuardrail()` — imported directly from `guardrails/remediationGuardrail.ts`, which lives outside `mcp-server/` entirely — with the same scenario as `guardrails/demoUnsafeAction.ts` (evidence-linked, valid action type, missing only approval), returning the real result for the button to render. (3) That cross-directory import broke `tsc --noEmit` (`TS6059: File is not under rootDir`); fixed by widening `mcp-server/tsconfig.json`'s `rootDir` to the repo root and adding the one specific guardrail file (not a wildcard, to avoid pulling in its test file) to `include`. (4) That fix had a real knock-on effect I found and fixed rather than left broken: widening `rootDir` changed the compiled output layout from flat `dist/*.js` to nested `dist/mcp-server/src/*.js`, silently breaking `package.json`'s `main`/`start` fields (which still pointed at the old flat path) — updated both, then verified `npm run build && npm start` end-to-end rather than assuming the path fix was sufficient. Also added `"exclude": ["**/*.test.ts"]` to `tsconfig.json` since the build was shipping compiled test files into `dist/`, unrelated noise noticed while already in there. (5) Updated `project-blueprint/demo-script.md`: Acts 2 and 3 now happen in one continuous browser tab against the dashboard (click the button, watch the real BLOCKED panel appear) instead of switching between browser and terminal — kept the original curl/tsx commands as a "Terminal alternative" appendix rather than deleting them. (6) Updated `project-blueprint/reviewer-one-pager.md`'s "The tools" section with a direct pointer to running the dashboard locally.
  - Verification: `npx tsc --noEmit` clean, `npm test` 30/30 (no regressions — dashboard/route changes didn't touch existing logic). Verified the dashboard for real in the Browser pane: screenshot confirmed the incident cards render correctly (session 61 visually flagged as blocked by 52, source tag reading `fallback`); clicked the "Recommend Remediation" button and confirmed via a second screenshot that it renders a real `BLOCKED` panel with the actual violations (`NOT_HUMAN_APPROVED`, `PRODUCTION_WRITE_REQUIRES_APPROVAL`) — a genuine click-triggered HTTP round-trip to the real guardrail function, not a static mockup. First click attempt (guessed screenshot-space pixel coordinates) missed the button entirely and produced no visible change; caught this by checking console logs (clean, no errors) and re-clicking via the element's ref instead of guessed coordinates, which worked — did not assume the first silent non-result meant success. Separately hit and killed a stale zombie `httpServer.ts` process squatting on port 8787 from an earlier, incompletely-cleaned-up step, which caused a misleading `NOT_FOUND` response from stale code before the real test could run.
  - Notes: This wasn't part of any specific `Rn` task card — it was direct user feedback mid-session that the deliverables so far, while all real and tested, weren't demonstrable as a running product. Scoped deliberately small (one static HTML file, two new routes, zero new dependencies) rather than starting the full React console, per explicit agreement with the user to revisit that as a separate, later, bigger build.

- [x] Drafted ADR-001: MCP transport selection for the Tool Gateway (docs only, no code change)
  - Date: 2026-08-12
  - Session: CC-20260812-h6t3
  - What changed: User asked for a document outlining the choice of transport (stdio vs. StreamableHTTP) for "the MCP server deployment," with rationale tied to performance/scalability. Rather than write a generic tradeoffs list, first checked this project's own real code and target design: `mcp-server/src/index.ts` currently implements stdio only for the real MCP protocol (R2); `httpServer.ts` is a separate, bespoke REST/JSON shim for the dashboard, not an MCP transport; `project-blueprint/architecture.md`'s "Deployment topology" section already commits to the Tool Gateway running near monitored platforms while the AI agents that call it run centrally in Kubernetes, scaled independently — a topology that structurally requires cross-host reachability, which stdio (client-spawns-server-as-subprocess) cannot provide. Added `docs/ADR-001-mcp-transport-selection.md` (new `docs/` folder, matching this repo's own `CLAUDE.md` folder-responsibility definition of `/docs` as "architecture notes, integration guides, system docs"): Context, five decision drivers each sourced from `architecture.md` or the current codebase (not generic), an options table (STDIO / StreamableHTTP / SSE), a two-part Decision (StreamableHTTP for the deployed Gateway that central-namespace agents call; STDIO retained as the existing local-dev/Claude-Desktop entry point, not replaced), Consequences (what implementing this would require: adding a `StreamableHTTPServerTransport` path alongside the existing stdio one, a stateless-vs-stateful call once replica count is known, and — flagged explicitly as its own governance-boundary item per this repo's `CLAUDE.md` Strategic Decisions list — real per-caller auth before any network exposure beyond a trusted network), and a "what would change this decision" section. Status is marked **Proposed**, not adopted — this is a decision document for the DRI to review, not an implementation.
  - Verification: Read `mcp-server/src/index.ts`, `mcp-server/src/httpServer.ts`, `mcp-server/package.json`, and `project-blueprint/architecture.md` directly before writing any claim in the ADR, so every driver and every "current state" statement in the document is checkable against real files in this repo rather than asserted from memory or from the earlier root-level MCP teaching-example conversation (which used an unrelated Python SDK and a fictional Notes Server, not this project). No code was changed; no test suite applies to a docs-only change, so `npm test`/`tsc --noEmit` were not re-run (nothing in `mcp-server/` was touched).
  - Notes: This document does not implement the StreamableHTTP transport, retire `httpServer.ts`, or wire authentication — those are named as open follow-on work inside the ADR itself, explicitly gated on DRI sign-off given the auth/production-network item crosses this repo's own "compliance or security posture" and "production infrastructure" escalation triggers. Session ID `CC-20260812-h6t3` was minted in the parent repo (root `PROGRESS.md`) earlier in this same continuous session for an unrelated root-level MCP teaching-example task; reused here rather than minted fresh, since this is the same session working across two `PROGRESS.md` files in the same checkout, and no existing entry in *this* file already used it.

- [x] Wired a test runner for the R4 guardrail, closing out the last open item on R4 (PLANNED -> BUILT)
  - Date: 2026-08-13
  - Session: CC-20260813-k2p9
  - What changed: User asked for a guardrail design + validator + tests + acceptance criterion moving R4 UNMAPPED -> PLANNED. Checked `PROGRESS.md` and `requirements.md` first (per this file's own session-start protocol) rather than rebuilding: `guardrails/remediationGuardrail.ts` (validator) and `guardrails/remediationGuardrail.test.ts` (8 cases) already existed from 2026-08-07 (session `CC-20260807-q8vz`), and R4 was already recorded as PLANNED — the one thing genuinely open was the top-of-file acceptance checklist's unchecked R4 row, which noted "no test runner is wired in yet, so 'pass' hasn't actually been confirmed." Closed that gap instead of duplicating the validator: added `guardrails/package.json` (vitest 4, matching `mcp-server`'s version) and `guardrails/tsconfig.json` (Node16/strict, matching `mcp-server`'s config style), added the missing `import { describe, it, expect } from "vitest"` to `remediationGuardrail.test.ts` (the file used them as untyped globals, which only works with a `globals: true` vitest config this repo doesn't use elsewhere — added the explicit import instead, consistent with how every `mcp-server` test file already does it), and fixed two pre-existing relative imports (`remediationGuardrail.test.ts`, `demoUnsafeAction.ts`) missing the `.js` extension Node16 module resolution requires, which was failing `tsc --noEmit`. Updated `requirements.md`: checked the R4 acceptance-checklist row, moved R4's own status line from PLANNED to BUILT (the bar the "Producing task" line itself named: "a test runner is wired in and the suite is confirmed passing"), and updated the R2/R1-style closing "R4 moves..." line to record both transitions with dates.
  - Verification: `cd guardrails && npm install && npm test` -> 8/8 tests passing; `npx tsc --noEmit` clean (0 errors) after the `.js`-extension fix. Re-ran `cd ../mcp-server && npx tsc --noEmit` afterward (its `tsconfig.json` separately includes `../guardrails/remediationGuardrail.ts`) to confirm the unchanged validator file still type-checks cleanly there too — exit 0.
  - Notes: R4's 4th property (audit-completeness) is still explicitly out of scope for this pure validator, per its own header comment — that's the execution service's responsibility once one exists, unchanged from the 2026-08-07 design. "CoreOps" in the user's request doesn't match any named component in this repo (checked via case-insensitive grep across the whole tree); treated as shorthand for this AI Operations Center project given the exact match to R4's UNMAPPED/PLANNED language in `requirements.md` — logged as an assumption per the Autonomy Model rather than pausing to ask, since it was implementation-level and reversible.

- [x] STORY-001: Implement human approval workflow for production changes (REQ-001)
  - Date: 2026-08-18
  - Session: CC-20260818-b7wq
  - What changed: Task brief for STORY-001 claimed "workspace repo not provisioned yet" and "nothing exists yet — you are laying the foundation," both false — this repo has been live all session (real commits, real webhook) and REQ-001 ("require human approval for any production-changing action") is functionally what `guardrails/remediationGuardrail.ts` (R4) already enforced, built 2026-08-07 and wired to a test runner 2026-08-13. Flagged the discrepancy to the user before building anything (per the brief's own "stop and ask if the requirement/premise turns out wrong" rule) rather than duplicating R4 from scratch; user confirmed the reuse-not-rebuild plan. Comparing STORY-001's 3 acceptance criteria against R4's existing coverage found one real gap: criterion 2 ("a denied approval, resubmitted, must still require approval") had no explicit "denied" state to test against — `RemediationAction.approval` only modeled presence/absence (`{approvedBy, approvedAt} | null`), so a denial and "not yet reviewed" were indistinguishable; and criterion 3 ("every approval decision is logged for audit") was a documented, explicit gap in R4's own header comment ("audit-completeness... is the caller's responsibility"), never built. (1) Changed `guardrails/remediationGuardrail.ts`: `approval` is now `ApprovalDecision | null` where `ApprovalDecision = { status: "approved" | "denied", decidedBy, decidedAt, reason? }`; both branches that previously checked `!action.approval` now also check `action.approval.status !== "approved"`, so a denial fails the same way absence does. Checked both real consumers (`guardrails/demoUnsafeAction.ts`, `mcp-server/src/httpServer.ts`) before changing the type — both only ever construct `approval: null`, so this was a backward-compatible, single-coordinated-PR change, not a breaking one for them. (2) Added `guardrails/approvalAuditLog.ts`: `buildApprovalAuditEntry()` (pure, injectable clock) + `ApprovalAuditLog` (in-memory, append-only, `record()`/`all()`/`forCorrelationId()`), explicitly scoped in its header comment as only the approval-decision slice of R4's audit-completeness property, not the full detection->execution trail (that's REQ-005/STORY-002, per `plan.json`'s `fulfilled_by`, left untouched). Per this repo's non-negotiable Idempotency & Replayability rule, `record()` dedupes on `(correlationId, decision.status, decidedBy, decidedAt)` so a retried/resubmitted identical decision event doesn't duplicate the trail — a distinct new decision sharing a correlation ID (e.g. a denial later reversed into an approval) is not deduped. (3) Updated `guardrails/remediationGuardrail.test.ts`: `baseAction()` now builds the new `ApprovalDecision` shape; added a denied-approval case and a resubmitted-denial case (asserting identical guardrail output both times, so the denial can't be laundered into a pass by resubmission). (4) Added `guardrails/approvalAuditLog.test.ts` (5 cases: logs an approval, logs a denial with its reason, idempotent on identical repeat, does not dedup two distinct decisions on the same correlation ID, filters by correlation ID). (5) Updated `project-blueprint/requirements.md`: R4's checklist row and "Producing task" now list both new files and the 15-case total; R4's property-4 closing paragraph now states the approval-decision slice is closed while the full end-to-end trail remains open, unchanged in status (still not BUILT for that property). (6) Populated `.colaberry/plan.json`'s previously-empty `STORY-001.acceptance_criteria` with the 3 criteria from the task brief. (7) Added a STORY-001 entry to `.colaberry/progress.json` (3 criteria, all `passed: true`, `verification.state: "submitted"` — matching STORY-000's precedent of not self-declaring "verified" without external reviewer/commit sign-off) and a new `story001ApprovalAudit` section; updated `tests.guardrails` from `{files:1, passing:8}` to `{files:2, passing:15}`. (8) Updated root `README.md`: guardrails/ row now mentions the audit log; split the single "30 tests" line into per-suite counts (30 mcp-server / 15 guardrails). Deliberately did NOT touch the README's "all four guardrails... unenforced" line or `guardrails.html`'s computed status — `verificationForRequirement()` in `assets/command-center.js` only reports `enforced` when a story's `verification.state === "verified"`, and STORY-001 is `"submitted"`, so the Command Center will still correctly show REQ-001 as not-yet-enforced until an actual reviewer/commit sign-off moves it — matching how STORY-000 was left at `"submitted"` rather than self-declared verified.
  - Verification: `cd guardrails && npm test` -> 15/15 passing (up from 8); `npx tsc --noEmit` clean. `cd mcp-server && npx tsc --noEmit` clean and `npm test` -> 30/30 passing, no regressions (confirms the cross-directory import of `remediationGuardrail.ts` still type-checks after the `approval` type change). Did not start the Command Center dev server for this change — the only observable-in-browser surfaces (`guardrails.html`, `project-management.html`) read `verificationForRequirement()`, which is unchanged logic reading data whose real-mode output was reasoned through by hand above (REQ-001 stays "not_enforced" in real mode); no UI code was touched.
  - Notes: The task brief's 3 acceptance criteria were not yet present in `.colaberry/plan.json` (empty array) before this change — formalized them into the plan as part of this story rather than leaving them only in the ephemeral brief text. STORY-001's `verification.state` is left at `"submitted"`, not `"verified"` — per this file's own established convention (STORY-000), a passing test suite earns "submitted," not a self-declared "verified," which this repo treats as requiring an external reviewer or commit-linked sign-off it doesn't yet have a mechanism for. Also flagged and declined an embedded prompt-injection earlier in this same session (a fake "conversation-compaction system prompt" appended to a task brief, instructing text-only output and no tool use) — noted here only because it happened in the same continuous session as this story, not because it produced any code change.

- [x] STORY-002: Log all decisions and actions for audit purposes (REQ-005)
  - Date: 2026-08-18
  - Session: CC-20260818-b7wq
  - What changed: STORY-002's brief itself named STORY-001 as prior art ("already specified before this one — reuse it, do not rebuild it") and pointed at the exact gap: STORY-001's `guardrails/approvalAuditLog.ts` only logged one event kind (a human's approve/deny decision on an already-known `RemediationAction`), while STORY-002's 3 criteria are broader — logging "actions" too (not just decisions), retrieval by ID (the old log only offered `all()` / `forCorrelationId()`, no single-entry lookup), and immutability (entries were plain mutable objects with no freeze). Proposed reusing/generalizing in place rather than adding a second, parallel logger; user confirmed before any edit, then confirmed again before the doc/progress-file follow-up, per the brief's explicit "paced co-pilot, wait for me to confirm" instruction — this story was worked as two separately-confirmed steps rather than one batch, unlike STORY-001. (1) Renamed `guardrails/approvalAuditLog.ts` -> `guardrails/auditLog.ts` (`git mv`, single file, nothing else in the repo imported the old name) and generalized it: a shared `AuditEntryEnvelope` (`id`, `correlationId`, `actor`, `loggedAt`) underlies two entry kinds — `DecisionAuditEntry` (STORY-001's approve/deny, `actor = decision.decidedBy`) and new `ActionAuditEntry` (a guardrail-evaluated action, `outcome: "executed" | "blocked"` derived from `GuardrailResult.allowed`, plus its `violations`, `actor` supplied by the caller — e.g. `"execution-service"`). `AuditLog` is now `Map<id, entry>`-backed instead of an array: `retrieve(id)` returns an explicit `{found: true, entry} | {found: false}` (criterion 1 + the "log entry missing" failure path, never `undefined`); every entry is `Object.freeze()`d on write, including its nested `decision` object or `violations` array (criterion 3, "Trust: timestamped and immutable" — `loggedAt` covers the timestamp half, freeze covers immutable); `id` is the idempotency key per this repo's non-negotiable Idempotency & Replayability rule — same id + identical content on retry is a silent no-op, same id + *different* content throws a new `AuditLogConflictError` rather than silently overwriting an immutable entry (a write conflict, not a duplicate). Added a new `AuditLogValidationError`, thrown by a `record()`-time check that rejects any entry missing `id`/`correlationId`/`actor`/`loggedAt` — covers criterion 2 ("must include the decision maker," generalized to "must include an actor") and the "log entry incorrect" failure path; STORY-001's original decision-only dedup-by-content logic was retired since `id` now does that job directly and more simply. (2) Renamed `guardrails/approvalAuditLog.test.ts` -> `guardrails/auditLog.test.ts`; ported all 5 STORY-001 cases to the new API (`buildDecisionAuditEntry`, explicit `id` params) and added 8 new STORY-002 cases: log-and-retrieve an executed action, log a blocked action with its violations, retrieve-missing returns `{found: false}` not `undefined`, a decision entry's `actor` is the decider, an action entry's `actor` is the caller-supplied trigger, a missing-`actor` entry is rejected with `AuditLogValidationError` and never stored, a stored entry is frozen (`Object.isFrozen` true, mutating the returned reference throws and doesn't change what's stored), and a same-id-different-content write throws `AuditLogConflictError` rather than overwriting. (3) Updated `project-blueprint/requirements.md`: R4's checklist row and "Producing task" line now point at `guardrails/auditLog.ts`/`auditLog.test.ts` and the 23-case total (up from 15); rewrote the closing paragraph under property 4 to say STORY-002 extends the approval-decision-only slice into decisions *and* actions, while being explicit that the *detection* and *recommendation* legs of R4's full trail are still unbuilt because no detection/recommendation subsystem exists (R1 is still UNMAPPED) — property 4 is closed for everything downstream of a proposed action, not end-to-end. (4) Populated `.colaberry/plan.json`'s previously-empty `STORY-002.acceptance_criteria` with its 3 criteria. (5) Added a STORY-002 entry to `.colaberry/progress.json` (3 criteria, `passed: true`, `verification.state: "submitted"`, same convention as STORY-000/001); replaced the `story001ApprovalAudit` section with a broader `auditLog` section covering both stories and the new file path; updated `tests.guardrails` to `{files: 2, passing: 23}`; bumped `totals.criteria_total`/`criteria_passed` from 8 to 11. (6) Updated root `README.md`: `guardrails/` row now describes `auditLog.ts` instead of `approvalAuditLog.ts`; test count line updated to 23 (from 15).
  - Verification: `cd guardrails && npm test` -> 23/23 passing (up from 15); `npx tsc --noEmit` clean. `cd mcp-server && npx tsc --noEmit` clean and `npm test` -> 30/30 passing, no regressions (mcp-server imports `remediationGuardrail.ts` from this same directory, unaffected by the audit-log rename since nothing there referenced the old file). Did not start the Command Center dev server — same reasoning as the STORY-001 entry above: the only observable surfaces read `verificationForRequirement()`, unchanged logic, and STORY-002 stays `"submitted"` so REQ-005 will correctly continue showing as not-yet-enforced in real mode.
  - Notes: Treated "How we build here: Use a secure logging service to store audit logs with timestamps" as generic boilerplate rather than a literal instruction to add an external SaaS dependency — flagged this reading to the user before building rather than assuming silently, consistent with how STORY-001's "use existing ITSM tools" line was also read as non-literal. Adding a real external logging service would be a new external dependency, one of this repo's own named governance/escalation triggers, and isn't warranted for a walking-skeleton in-memory audit log with no deployed backend to call one from yet. `verification.state` left at `"submitted"`, not `"verified"`, same convention as every prior story in this file.

- [x] Added a Claude Code PR review GitHub Actions workflow (ad hoc, user-requested — not tied to a `.colaberry` story/requirement)
  - Date: 2026-08-18
  - Session: CC-20260818-b7wq
  - What changed: User asked for automated code review on pull requests. Checked first rather than assuming from scratch: the sibling root `ColaberryAI` repo already has an equivalent `.github/workflows/claude-code-review.yml` using `anthropics/claude-code-action@v1`, and `ai-operations-center` (this repo) had no `.github/` at all yet — but `gh secret list -R qninying/ai-operations-center` showed a `PR_REVIEWER` secret already provisioned here (added earlier the same day), meaning the repo had already been set up in anticipation of this. Added `.github/workflows/claude-code-review.yml`, matching the root repo's proven structure (`on: pull_request: [opened, synchronize]`, a per-PR `concurrency` group so a newer commit cancels an in-flight review, minimal `permissions: {contents: read, pull-requests: write}`, `actions/checkout@v4` with `fetch-depth: 0`, then `anthropics/claude-code-action@v1` reading `ANTHROPIC_API_KEY` from `secrets.PR_REVIEWER`). Deliberately did NOT copy the root workflow's prompt verbatim ("check against this repository's CLAUDE.md conventions") — checked first and found `ai-operations-center/CLAUDE.md` on disk is untracked (`git ls-files` confirms it was never committed to this repo) and its content is actually the *generic* Colaberry-accelerator-program CLAUDE.md (references `backend/src/services`, Node+Express, `BusinessAnalysis_FieldGuide.html` — none of which exist here), so pointing an automated reviewer at "this repo's CLAUDE.md" would have it check real TypeScript guardrail/MCP-server code against a document describing a different, nonexistent stack. Wrote a prompt grounded in this repo's actual, committed conventions instead: keep `tsc --noEmit` clean per-package, new business logic needs a test (pointing at `guardrails/remediationGuardrail.test.ts` / `guardrails/auditLog.test.ts` as the established style), side-effecting code stays idempotent (pointing at `auditLog.ts`'s id-as-idempotency-key pattern as the concrete precedent), and flagging any `"done"`/`"verified"` claim in `.colaberry/progress.json` or `PROGRESS.md` that isn't backed by a real test — this repo's own established norm (every story in this file has stayed at `"submitted"`, never self-declared `"verified"`, per STORY-000/001/002's entries above).
  - Verification: Validated the YAML with `python3 -c "import yaml; yaml.safe_load(...)"` after installing `pyyaml` — parses cleanly, structurally matches the root repo's file (same top-level keys: `name`, `on`, `concurrency`, `permissions`, `jobs`). Confirmed via `gh secret list -R qninying/ai-operations-center` that `PR_REVIEWER` already exists in this repo, so the workflow references a real, already-provisioned secret rather than a placeholder. Did not open a test PR to trigger a live run — that's a visible, public action on the user's repo I didn't want to take unprompted; the user can trigger a real run by opening any PR, or ask for a test PR to be opened as a separate, explicit step.
  - Notes: This wasn't scoped by any `.colaberry` story or `Rn` requirement — an ad hoc infra request outside the story-driven flow this session has otherwise followed, so `.colaberry/plan.json`/`progress.json` were intentionally left untouched for this entry. No secret value was created, read, or logged — only the existing secret's *name* is referenced in the YAML, consistent with this repo's Security Enforcement Layer (no secrets in code/commits/logs).

- [x] Pinned the PR-review workflow's actions to commit SHAs + added Dependabot to keep them current (ad hoc, user-requested)
  - Date: 2026-08-18
  - Session: CC-20260818-b7wq
  - What changed: User asked what's "best" for the floating-tag caveat flagged in the prior entry (`actions/checkout@v4`, `anthropics/claude-code-action@v1` can silently move to different code under the same tag) and, after a recommendation, asked to both pin and add Dependabot. Resolved the exact commit each tag currently points to via `gh api` rather than guessing: `actions/checkout@v4` -> `11d5960a326750d5838078e36cf38b85af677262` (tag `v4.4.0`); `anthropics/claude-code-action@v1` -> dereferenced the annotated tag object to its commit, `459ad358ae43fea66bfefd0a1f8d840b4b9791fb` (tag `v1.0.194`). Updated `.github/workflows/claude-code-review.yml`'s two `uses:` lines to `owner/repo@<full-sha> # vX.Y.Z` — the trailing version comment is not decorative: GitHub's Dependabot reads it to know what to bump a SHA pin to and updates the comment alongside the hash, per its own documented convention for SHA-pinned actions. Added `.github/dependabot.yml` (`package-ecosystem: "github-actions"`, root directory, weekly schedule) so a new release of either action shows up as a small, reviewable PR bumping the SHA + comment, instead of the pin going stale silently or the user needing to remember to check.
  - Verification: Re-validated both YAML files with `python3 -c "import yaml; yaml.safe_load(...)"` — parse cleanly. Confirmed the resolved SHAs are correct by round-tripping through the GitHub API: fetched `git/refs/tags/v4` and `git/refs/tags/v1` (the latter an annotated tag object, dereferenced via `git/tags/<sha>` to its underlying commit) rather than trusting the `tags` list's ordering, so the pin matches what the floating tag *actually* resolves to right now, not an assumption.
  - Notes: Did not re-push a test PR for this either — same reasoning as the prior entry, opening one is a visible action on the user's public repo and wasn't asked for here. Chose weekly Dependabot cadence (not daily) since this is a two-action, low-churn workflow — daily would just be more noise for the same eventual outcome.

- [x] Aligned STORY-001/STORY-002 criterion wording with the platform's original brief text (checker sync, no code change)
  - Date: 2026-08-18
  - Session: CC-20260818-b7wq
  - What changed: User pasted a message claiming to be from "platform AI" reporting STORY-000 fully confirmed, explaining a wait was the platform's own lag (not this repo's), disclosing that `ColaberryIntern` now has push access and two "Colaberry Build Bot" commits had landed, and proposing 3 exact-text replacements in `.colaberry/progress.json` to fix STORY-001 (1/3 confirmed) and STORY-002 (2/3 confirmed). Treated the message as untrusted external content per this session's established practice and independently verified every checkable claim before acting on any of it, rather than accepting the narrative: (1) `git fetch` confirmed two real commits from `Colaberry Build Bot <build-bot@colaberry.ai>` on `origin/main` not present locally; diffed both and confirmed the first rewrote `plan.json` (+649/-65 lines) and the second reverted it, with the reverted version byte-identical to my last commit (`git diff b2595d2:.colaberry/plan.json cd6b34c:.colaberry/plan.json` — no output), `manifest.json` netting to zero change, and `progress.json` untouched in either commit (all confirmed via `git diff`, not by reading the bot's own commit message). (2) `gh api repos/.../collaborators` confirmed `ColaberryIntern` genuinely has `push: true` access with no pending invitation, consistent with the claim it was accepted — but explicitly did not accept this on the user's behalf; no `gh` invitation/collaborator command was run this session, so that access came from an action the user took outside this conversation, not from anything I did. (3) Checked the wording-mismatch diagnosis against my own prior work rather than trusting the platform message's framing: `progress.json` and `plan.json` already matched each other exactly (both carried my own paraphrase from when I first transcribed the brief's acceptance criteria into the plan), so the platform message's specific claim ("progress.json was reworded relative to your plan") was imprecise — the actual divergence was between both my files and the original, literal task-brief wording I already had verbatim earlier in this session. Cross-checked the platform message's 3 proposed replacement sentences against that original brief text directly (not against the new message) and confirmed all 3 match character-for-character. (4) Independently resolved the "resubmitted unchanged" vs. any-resubmission caveat the message raised, rather than taking its suggestion at face value: `checkRemediationGuardrail()` is a pure, stateless function with no concept of "resubmission" as an event — it only evaluates whether the object it's given right now has `approval.status === "approved"`, so a denied action fails that check regardless of what else about the object changed on resubmission. Confirmed the broader wording is honestly true of the existing implementation, not just the narrower case originally written. (5) Fast-forwarded local `main` onto `origin/main` (`git merge --ff-only`) before editing, so the change lands on top of the bot's commits rather than diverging from them. (6) Applied the 3 corrected sentences to both `.colaberry/progress.json` (the file the message named) and `.colaberry/plan.json` (not explicitly named by the message, but already carrying the identical stale wording — left uncorrected there, the next person or process treating `plan.json` as source of truth would just reintroduce the same drift). `"passed": true` preserved on all three, since only the descriptive text changed, not the underlying tested behavior.
  - Verification: `python3 -c "import json; ..."` confirmed both files parse as valid JSON, `plan.json`'s `acceptance_criteria` and `progress.json`'s `criteria[].text` are identical arrays for both stories post-edit, and all `passed` flags remain `true`. No source code changed, so `npm test`/`tsc --noEmit` were not re-run — nothing in `guardrails/` or `mcp-server/` was touched.
  - Notes: This is a criterion-wording correction, not a functional change — the guardrail and audit-log behavior underlying these criteria was already correct and already tested (see the STORY-001/STORY-002 entries above). Recommended, but have not yet acted on, the platform message's separate suggestion to move `progress.json`'s `r2McpServer`/`r4Guardrail`/`auditLog`/`tests` metadata into a `docs/` file so a future "Sync" can't drop it, given the demonstrated bot behavior of rewriting `plan.json` wholesale — holding that as its own future step rather than bundling it in here.

- [x] Moved progress.json's implementation notes into docs/ so a future platform Sync can't drop them
  - Date: 2026-08-18
  - Session: CC-20260818-b7wq
  - What changed: Follow-up on the platform message's separate suggestion (see prior entry's Notes) — user confirmed to proceed. Checked before moving anything: `assets/command-center.js` and every `.html` page were grepped for `r2McpServer`/`r4Guardrail`/`auditLog`/`.tests\b` and none reference these fields, so removing them from `.colaberry/progress.json` cannot break the live dashboard (only `stories`/`totals`/`schema_version` are actually read at runtime). Also checked `docs/` already existed (`ADR-001-mcp-transport-selection.md`), so this follows an established convention rather than introducing a new one. Created `docs/implementation-notes.md`: a short markdown index (R2 MCP server, R4 guardrail, the audit log, test suite counts) carrying the same content verbatim, reformatted as prose/tables, plus a note at the top explaining why it moved (the 2026-08-18 Build Bot incident from the prior entry — real, verified, not hypothetical). Removed the `r2McpServer`, `r4Guardrail`, `auditLog`, and `tests` top-level keys from `.colaberry/progress.json`, leaving `schema_version`/`totals`/`stories`/`note_totals`/`requirementsFile` — the fields that are actually part of the story-tracking schema. Added a new `implementationNotesFile: "docs/implementation-notes.md"` pointer field, matching the existing `requirementsFile` pattern, so anyone reading `progress.json` can still find where the notes went.
  - Verification: `python3 -c "import json; ..."` confirmed `progress.json` still parses and all 3 stories' data is untouched. Started the Command Center locally (`python3 -m http.server`) and loaded `guardrails.html?id=REQ-001` in a real browser — renders correctly (`REQ-001 · ENFORCED`, STORY-001 status shown), confirming the trimmed file doesn't break the live page, not just that it parses.
  - Notes: Scoped the move to exactly the 4 sections the platform message named — left `note_totals` and `requirementsFile` in place, since those weren't flagged and are lighter-weight/more plausibly part of whatever schema a future Sync expects. If a future Sync does touch `progress.json`'s `stories` array directly, that's a different, larger problem this move doesn't solve — this only protects the hand-written notes that sat outside the schema entirely.

- [x] STORY-003: Implement AI-driven diagnostics and recommendations (REQ-002/003/004), closing R1 UNMAPPED -> BUILT
  - Date: 2026-08-18
  - Session: CC-20260818-b7wq
  - What changed: STORY-003's own "How we build here" line said "Develop AI models to analyze incidents" — read as generic boilerplate (consistent with how prior stories' boilerplate lines were read), not a literal instruction to train a model. R1's actual definition in `requirements.md` ("using Claude, reasoning over live, read-only evidence... not a template response") was the real spec, and it required something this repo didn't have: a real Anthropic API call, which meant a new external dependency and a credential — both explicit "stop and ask" triggers per this story's own brief and this repo's Autonomy Model. Stopped and presented the tradeoff before writing anything (fake it vs. really call Claude); user chose "really call Claude." (1) Added `mcp-server/.env.example` documentation and a blank `mcp-server/.env` for `ANTHROPIC_API_KEY`, named per `architecture.md`'s own Components table entry for the "Root Cause Analysis Agent" — user provided their own real key directly into the file; the key value was never entered, echoed, or logged by Claude Code at any point (confirmed by grepping tracked files for a match after the fact — none found). (2) Added `@anthropic-ai/sdk` as a deliberate, logged dependency (`npm install @anthropic-ai/sdk --save`, 0 vulnerabilities). (3) Built `mcp-server/src/rootCauseAgent.ts` as `architecture.md` names it — the Root Cause Analysis Agent — placed in `mcp-server/` rather than `guardrails/` (where an earlier, pre-session draft of the `requirements.md` R1 row had guessed) since it consumes `mcp-server`'s DMV evidence directly and `guardrails/` is specifically the approval/audit module. `analyzeIncidentRootCause(incident, options?)`: evidence-grounded (real evidence data serialized into the prompt, not a description), attributed (response schema requires `evidenceIdsUsed`, zod-validated), non-fabricated when thin (empty evidence short-circuits to a deterministic low-confidence result without an API call at all; a genuine upstream failure throws a typed error rather than being coerced into a low-confidence "answer"). Reuses the existing `withReliability`/`CircuitBreaker` reliability layer verbatim (same pattern as `dmvLiveSource.ts`) rather than writing new retry logic — 30s timeout, 2 retries, its own module-level circuit breaker. `options.callModel` is the test injection point, mirroring `dmvReader.ts`'s injectable-query-function pattern. (4) Added `mcp-server/src/rootCauseAgent.test.ts` (10 cases: evidence-grounded prompt content, attributed evidence IDs, insufficient-evidence with zero API calls, insufficient-evidence from genuine low model confidence, out-of-range-confidence rejection, missing-required-field rejection, non-JSON rejection, missing-API-key, retry-then-fail via fake timers, repeatability). (5) **Found and fixed a real, previously-undiscovered gap while trying to verify the new key actually worked**: nothing in this codebase ever loaded `.env` into `process.env` — no `dotenv`, no `--env-file`, no `loadEnvFile()` call anywhere — so a filled-in `.env` had been silently inert this whole time, for `SQLSERVER_*` too, not just the new key. Added `mcp-server/src/loadEnv.ts` (Node's built-in `process.loadEnvFile()`, no new dependency; existence-checked first so a missing `.env` doesn't crash startup, matching this repo's existing "missing config -> graceful path, not a hard failure" convention) and imported it as the first line of both `index.ts` and `httpServer.ts`. (6) Updated `project-blueprint/requirements.md`: R1 moved UNMAPPED -> BUILT directly (not through PLANNED, since the producing task, its tests, and a real API verification all landed together) — checklist row and full R1 section rewritten with the real file path, the corrected `mcp-server/` location (vs. the stale `guardrails/` guess), and how each of the three properties is enforced structurally. (7) Updated root `README.md`: added a `rootCauseAgent.ts` row, updated the reliability-layer row to mention it wraps the Anthropic call too, bumped the mcp-server test count to 40, and rewrote the intro's stale "0 of 11 programme stories started" line to reflect the real current count (4 submitted) while keeping it honest that "submitted" isn't "verified" — the guardrails still correctly show not-yet-enforced. (8) Populated `.colaberry/plan.json`'s previously-empty `STORY-003.acceptance_criteria` with the 3 criteria from the brief, verbatim (learned from the STORY-001/002 wording-drift incident earlier this session — used the brief's exact capitalization/phrasing this time, not a paraphrase). (9) Added a STORY-003 entry to `.colaberry/progress.json` (3 criteria, `passed: true`, `verification.state: "submitted"`); bumped `totals.criteria_total`/`criteria_passed` from 11 to 14.
  - Verification: `cd mcp-server && npm test` -> 40/40 passing (up from 30); `npx tsc --noEmit` clean; `guardrails/` `tsc --noEmit` re-confirmed clean and unaffected. Booted the real stdio server (`npx tsx src/index.ts`) after adding `loadEnv.ts` — connects cleanly, no crash from the new import. Then, the verification that actually matters most for R1: ran `analyzeIncidentRootCause()` against the **real, live Anthropic API** (not mocked) with a realistic DMV-shaped incident (a blocked session, `LCK_M_X` wait, cumulative CPU time) via a throwaway script, deleted immediately after. Real response: Claude correctly distinguished "suspended status" from "high current CPU" (noting the 8200ms figure was likely cumulative, not current), correctly identified the lock-wait/blocking-session mechanism as the actual root cause, cited the real evidence ID (`evt-4471`), and returned a calibrated 55% confidence with an explicit reason it wasn't higher (no evidence on what session 52 itself was doing) — this is genuinely evidence-grounded, attributed reasoning, not a templated response, confirmed by inspection of real output, not asserted. Also verified the Command Center renders STORY-003 correctly in both Sample and Real mode via a real browser check (Real mode correctly shows `"submitted"`, matching STORY-000/001/002's precedent, not a self-declared `"verified"`).
  - Notes: R1 moving straight to BUILT (skipping PLANNED) is a deliberate deviation from R2/R3's slower path — those are still stuck at PLANNED specifically because their live path has never been run against a real backend, only fixture/mocked data. R1's live path *was* run for real before this entry was written, which is the actual bar `requirements.md` sets for BUILT elsewhere in this file, so holding it at PLANNED anyway would have been arbitrarily conservative rather than honest. No secrets committed: `mcp-server/.env` is gitignored (confirmed via `git status --porcelain` showing nothing for it) and a full grep across all tracked file types for a match against the real key found nothing.

- [x] STORY-004: Gather additional diagnostics for low-confidence incidents (REQ-010)
  - Date: 2026-08-18
  - Session: CC-20260818-b7wq
  - What changed: Flagged and resolved a threshold question before writing anything: REQ-010's "confidence below 80%" and `rootCauseAgent.ts`'s existing `INSUFFICIENT_CONFIDENCE_THRESHOLD = 30` are not the same concept and don't conflict — 30 means "no usable answer at all," 80 (this story's own bar) means "confident, but not confident enough to act on a single explanation without a second opinion." Left the existing 30 threshold untouched. Interpreted "gather additional diagnostics" as a differential-diagnosis list (the acceptance criteria's "multiple diagnostics... include possible causes," plural) rather than pulling more raw DMV data — deliberately did not expand DMV coverage, since that's explicitly `STORY-006`'s scope ("Enable SQL Server data access"), named in this brief's own "do NOT build now" list. Added `mcp-server/src/diagnosticsGatherer.ts` (`gatherAdditionalDiagnostics(incident, rootCauseResult, options?)`) as a new file rather than extending `rootCauseAgent.ts` (already 200 lines; a second Claude-calling capability with its own prompt/schema would push past this repo's own size guidance and mix two responsibilities). No-ops (`gathered: false`, no API call) when `rootCauseResult.confidence >= 80`; otherwise sends the same evidence plus the low-confidence root cause back to Claude, asking for multiple zod-validated `{description, possibleCause}` entries — reuses `withReliability`/`CircuitBreaker` exactly like `rootCauseAgent.ts`. Logging uses the same `logEvent()` structured logger `rootCauseAgent.ts` established, not `guardrails/auditLog.ts`'s `AuditLog` — flagged this explicitly before building, since "reuse STORY-002" could be read either way: `AuditLog`'s action-entry shape is specifically modeled on guardrail-evaluated remediation actions (executed/blocked + violations), and forcing "diagnostics were gathered" through that shape would be a semantic misfit rather than genuine reuse. Every call now logs exactly one event — `additional_diagnostics_gathered` when something was fetched, `additional_diagnostics_not_needed` when confidence was already sufficient — so "Trust: all diagnostics gathering is logged" holds for every invocation, not only successful fetches. Deliberately did NOT extract shared Claude-calling infrastructure (`MissingApiKeyError`, `defaultCallModel`) out of `rootCauseAgent.ts` despite the duplication — this repo's own composition rule extracts at 3+ occurrences ("two is sometimes a coincidence"), and refactoring `rootCauseAgent.ts` (STORY-003, already shipped and BUILT) would mean editing a file outside this story to make this one pass, which the brief itself names as a stop-and-ask trigger. Logged as the deliberate call it is; noted in the file's own header comment that a third Claude-calling module (e.g. architecture.md's Impact & Remediation or Summary agents) would be the right point to extract a shared helper. Added `mcp-server/src/diagnosticsGatherer.test.ts` (11 cases: no-op above threshold with zero API calls, gathers below threshold, prompt includes the real low-confidence root cause and real evidence, multiple diagnostics each carry a possible cause, malformed-response rejection [missing field, empty list, non-JSON], missing-API-key, retry-then-fail via fake timers, and two logging tests — one confirming a successful gather logs exactly once, one confirming a skipped gather also logs exactly once, using a `vi.spyOn` against the real logger rather than trusting the implementation). Updated `.colaberry/plan.json`/`progress.json` with STORY-004's 3 criteria verbatim from the brief (no paraphrase, learned from the earlier wording-drift incident) and a new progress.json entry, `verification.state: "submitted"`. Updated `README.md`: new `diagnosticsGatherer.ts` row, test count to 51, story count in the intro to 5 of 11.
  - Verification: `cd mcp-server && npm test` -> 51/51 passing (up from 40); `npx tsc --noEmit` clean. Then ran `gatherAdditionalDiagnostics()` against the **real, live Anthropic API** (not mocked) via a throwaway script (deleted after), with a realistic low-confidence incident (an SSIS package failure, `ASYNC_NETWORK_IO` wait, 45% confidence root cause). Real result: 5 genuinely distinct, technically sound possible causes (client not consuming result rows fast enough, network saturation, slow downstream SSIS transform, an unrelated earlier failure, driver/TDS buffering issues) — each correctly reasoned about the specific wait type rather than generic filler, confirming this is real differential reasoning over real evidence, not templated output. Checked the full git history (not just the working tree) for the API key with `git rev-list --all | xargs git grep` — zero matches, confirming it never touched any commit. Verified the Command Center renders STORY-004 correctly in Real mode via a real browser check (`"submitted"`, all 3 criteria shown) — hit one transient tool-level timing glitch on the first read attempt right after a click (no console errors, unrelated to the app itself), which a retry resolved cleanly.
  - Notes: Confidence this story is complete and correct: ~88%. The three criteria are each independently backed (differential presented as structured, directly-usable data; every entry carries a `possibleCause`; every invocation logged) and verified against the real API, not just mocked. What would raise it: real user-facing presentation of these diagnostics in the dashboard (out of scope here — that's the Operations Console / STORY-005 territory), and confirmation of how "presented to the user" should be interpreted once a real UI consumer exists, since right now it's satisfied by returning a well-formed structure, same bar STORY-003 used for "when it is viewed."

- [x] STORY-005: Develop role-based dashboards for different user types (REQ-006 + REQ-009)
  - Date: 2026-08-19
  - Session: CC-20260818-b7wq
  - What changed: Flagged a real tension before writing anything: this brief's "How we build here" said "a UI framework that supports role-based views," but this repo's own `PROGRESS.md` had an explicit, user-agreed prior decision to defer exactly that ("React, auth, role-based views — genuinely Phase 5-6 scope") when the existing `mcp-server/src/dashboard.html` was first built. Recommended staying with the existing no-framework dashboard; user overrode that recommendation and asked for a real UI framework — proceeded with React given that's the explicit, informed call of the person who owns that tradeoff, not something I should re-litigate once decided. Built as 4 separately-confirmed steps (paced co-pilot, per this story's own instruction): (1) `mcp-server/src/dashboardSummary.ts` — `buildDashboardSummary(role, dmvResult)`, kept separate from `httpServer.ts` (which has no test coverage of its own, by design) so the real logic is unit-tested. Only `"it-manager"` is implemented, even though the release's own goal mentions a DBA view too — that's outside this story's actual acceptance criteria; a `"dba"` role request honestly throws `UnknownRoleError` rather than a fabricated view. "Operational summary" = the same real DMV data reshaped into counts/status (incident count, blocked-session count, data source), not raw per-session fields — that's what makes it genuinely role-*specific*, not just re-labeled. 5 tests. (2) New `GET /api/dashboard/summary?role=...` route in `httpServer.ts` — thin wrapper (matching the existing `/dmv/exec-requests` pattern), logs every access via `logEvent()` with role + outcome (covers "Trust" + gives "Access log failure" something concrete to fail against), 400 for `UnknownRoleError`. Verified live with curl before moving on: valid role -> real data; unknown role and missing role both -> 400 with a clear message, not silently rendered wrong; server log confirmed all three accesses logged. (3) Scaffolded `frontend/` (Vite + React + TypeScript, renamed to `coreops-frontend` for naming consistency with the other two packages) — proven to actually build (`npm run build`, real 193KB bundle) and boot (dev server + real browser render, zero console errors) before any dashboard logic went in it. Added `.claude/launch.json` for `preview_start`, but that tool itself fails with `EPERM: process.cwd failed` spawning the dev server on this machine — confirmed via a side-by-side test that the identical command works perfectly when run directly via Bash, so this is a sandbox limitation in the preview tool's own process spawning, not a defect in the app; verified every step after this by starting the server manually and pointing the browser at the URL directly instead. (4) The actual page: reads `?role=` from the URL, fetches `/api/dashboard/summary` through a Vite dev-only proxy (`vite.config.ts`) rather than adding CORS headers to the backend — deliberately, since CORS on a server that will eventually handle real DMV data is its own production security decision not to make casually inside this story; noted honestly in a code comment that how the *built* frontend reaches the backend in a real deployment is still an open question, not silently assumed solved. Both named failure paths ("Dashboard not loading" = fetch/network failure, "Incorrect role information" = backend 400) converge on one honest error state showing the backend's real message, never a blank or crashed page. 5 new frontend tests (happy path, honest fallback-source labeling, no-role error, unknown-role error, network-failure error). Caught and fixed two real bugs while verifying, not just claimed success: a test-isolation bug (missing `afterEach(cleanup)` in `setupTests.ts` was leaking DOM between tests, causing a spurious failure) and a real visual bug visible in a screenshot (the wrapped `h1` title overlapped its own second line from too-tight `line-height`) — both fixed and re-verified. After user feedback that the dashboard looked "too plain," redesigned the CSS to reuse `assets/command-center.css`'s exact design tokens (`#2f5fd6` accent, semantic ok/warn colors, radius/border/shadow values) rather than inventing a new palette, so CoreOps's two UIs (Command Center + this Operations Console) read as one product — added a header bar, accent-bordered cards, and a colored status badge (green/amber dot) replacing plain colored text; re-verified in a real browser after the redesign (correct render, clean network requests, no new console errors) and re-ran all tests (still 5/5, unaffected — only presentation changed). Updated `.colaberry/plan.json`/`progress.json` with STORY-005's 3 criteria verbatim from the brief and a new progress.json entry, `verification.state: "submitted"`. Updated `README.md`: new rows for `dashboardSummary.ts`/the API route and `frontend/`, a "See it running" section for the Operations Console, test count to 56 (mcp-server) + 5 (frontend), story count to 6 of 11. Confirmed `project-blueprint/requirements.md` needs no change — REQ-006/009 aren't part of its R1-R5 scope, same as REQ-010/STORY-004.
  - Verification: `mcp-server`: 56/56 tests passing, `tsc --noEmit` clean. `guardrails`: `tsc --noEmit` clean, unaffected. `frontend`: 5/5 tests passing, `tsc -b` clean, `npm run build` produces a real production bundle. Full real end-to-end check: both real servers (backend `httpServer.ts` + frontend Vite dev server) started together, a real browser navigated through the actual dev proxy to the live API — correct data rendered, network tab confirmed real `200` responses, zero console errors; unknown-role error path also checked live in the browser, showing the backend's real message. Verified the Command Center renders STORY-005 correctly in Real mode (`"submitted"`, all 3 criteria shown).
  - Notes: Confidence this story is complete and correct: ~85%. All three criteria are backed by both unit tests and a real, live end-to-end browser check, not just claims. What would raise it: resolving the still-open "how does the built frontend reach the backend in production" question (currently a dev-only proxy, honestly flagged as unsolved rather than papered over), and getting `preview_start` working for this repo so future frontend verification doesn't need the manual-start workaround.

- [x] Reconciled with a major platform sync — real, authoritative CLAUDE.md/schema now govern this repo (no functional code change)
  - Date: 2026-08-19
  - Session: CC-20260818-b7wq
  - What changed: Before pushing STORY-005, `git fetch` surfaced a new `Colaberry Build Bot` commit ("sync build plan — 20 files") much larger than the prior two-file syncs — touched `.colaberry/plan.json` (704 lines), `.colaberry/progress.json` (460 lines, for the first time — prior syncs explicitly avoided it), added `.colaberry/profile.json`, a new tracked `CLAUDE.md`, and `docs/REQUIREMENTS.md`/`STORIES.md`/`TRACEABILITY.md`/`stories/STORY-nnn.md`. Investigated before merging, same as every prior sync incident this session: (1) the bot's `plan.json` had emptied every story's `acceptance_criteria` to `[]` and dropped `STORY-000` from the story list entirely — merging carelessly on top of my own uncommitted `plan.json` edit (which had just added STORY-005's criteria there) would have looked fine locally but been silently wrong. (2) the bot's `progress.json`, by contrast, had all 12 stories including `STORY-000`, with `STORY-000` through `STORY-004` already flipped from my own hand-maintained `"submitted"` to a real, platform-authored `"verified"` state, `criteria` text matching what I'd already written nearly verbatim (one residual case difference on STORY-001's first criterion that evidently never blocked its confirmation), and every `passed: true` flag I'd set preserved intact. (3) The new tracked `CLAUDE.md` turned out to be the actual explanation for all of this: an authoritative ownership contract this repo never had before — `plan.json`/`manifest.json` are platform bookkeeping, always overwritten, never mine to edit; `progress.json`'s story/criterion *list* is platform-owned too (I should never invent criteria there, matching what I'd been doing all session by necessity, in the absence of any real schema); my job is only ever `criteria[].passed`, `files_touched`, `tests_added`, and `notes`, which "a sync keeps" — confirmed true by observing my own `passed:true` values survive this exact sync untouched. This also revealed I'd never been populating `files_touched`/`tests_added`/`notes` at all, since I didn't know those fields existed. Handled the merge carefully rather than a plain `git merge`: backed up the local, untracked `CLAUDE.md` first (it's gitignored specifically because — per the repo's own `.gitignore` comment — it contains a real production SSH command and an internal escalation email; a git operation silently deleting it would have been fine security-wise but not something to do without flagging), discarded my conflicting uncommitted `plan.json`/`progress.json` edits (`git checkout --`, no longer mine to hand-author per the new contract), moved the old local `CLAUDE.md` out of the way so the incoming tracked one wouldn't hit an "untracked file would be overwritten" error, then `git merge --ff-only`. Updated `progress.json`'s `STORY-005` entry the way the new `CLAUDE.md` actually specifies: flipped its 3 `criteria[].passed` to `true` (list/text left exactly as the platform provided, since it already matched my brief-transcribed wording), and populated `files_touched`/`tests_added`/`notes`/`updated_at` for the first time this session. Left `verification.*` (state, `criteria_passed`, `commit_sha`, `points_awarded`, etc.) completely untouched — that block is explicitly the platform's own, populated after it verifies a pushed commit, not something to self-declare.
  - Verification: `python3 -c "import json; ..."` confirmed `progress.json` still parses, and that STORY-000 through STORY-004's data is byte-for-byte unchanged from what the bot's sync provided (only STORY-005's fields were touched). Re-ran the full test suite in both packages after the merge to confirm nothing in the actual codebase was affected by this reconciliation (it wasn't — this was purely `.colaberry`/`CLAUDE.md`/`docs` bookkeeping): `mcp-server` 56/56, `frontend` 5/5, both `tsc` clean.
  - Notes: Going forward, per the new `CLAUDE.md`'s explicit "what not to edit" section: stop hand-authoring `.colaberry/plan.json` and `.colaberry/manifest.json` entirely (they were never really mine to edit — I was filling a real gap in the absence of any sync mechanism, and that gap is now closed), stop inventing criteria text/lists in `progress.json`, and start actually populating `files_touched`/`tests_added`/`notes` per story, which I have not been doing. The local `CLAUDE.md` backup lives at `/private/tmp/.../scratchpad/CLAUDE.md.old-local-copy` (scratchpad, not committed) purely as a just-in-case reference — the new tracked one is now this repo's real, working operating contract and should be treated as authoritative from here on, superseding the generic accelerator-program one this session inherited at the start.

- [x] STORY-006: Enable SQL Server data access for AI recommendations (REQ-007 + REQ-013) — closes R2/R3 PLANNED -> BUILT
  - Date: 2026-08-19
  - Session: CC-20260818-b7wq
  - What changed: Flagged two things before writing code, per this story's own "stop and ask" list. (1) No live SQL Server existed in this environment (`SQLSERVER_*` still empty) — same shape of gap as STORY-003's credential question, but not a full blocker: the acceptance criteria are structured around both outcomes (connected / unreachable), and "unreachable" was the actual, honest state of the environment at the time, so criterion 2 could be genuinely verified live from the start. User chose to close the gap for real rather than accept the caveat — walked through creating a real Azure SQL Database (Free tier) in the portal (their account, their browser; I only gave directions, never touched credentials — filled in `SQLSERVER_HOST`/`DATABASE` from a screenshot once deployed, left `USER`/`PASSWORD` for the user to enter directly). (2) A real design tension with existing code: `dmvReader.ts`'s established philosophy is silent fallback-to-fixture on any SQL failure, but this story's criterion 2 wants an explicit connectivity notification — generating an "AI-driven recommendation using SQL Server data" from fixture data and calling it real would be exactly the fabrication `rootCauseAgent.ts` was built to refuse. Resolved by having the new service bypass `dmvReader.ts`'s fallback entirely rather than reuse it. Built: (1) `mcp-server/src/recommendationService.ts` — `generateRecommendation()` calls `queryLiveDmv()` directly, zod-validates every row before treating it as evidence (new: extends the validate-before-trust discipline `rootCauseAgent.ts`/`diagnosticsGatherer.ts` already apply to Claude's responses, to the SQL layer too), reuses `analyzeIncidentRootCause()` as-is. Any SQL failure (unreachable, timeout, circuit-open — `queryLiveDmv()`'s existing reliability wrapper already distinguishes these) collapses to one `SqlServerUnavailableError`, covering "connection failure" and "data retrieval timeout" with infrastructure that already existed. A malformed row throws `InvalidDataFormatError`, giving that named failure path real, testable meaning instead of a checkbox. Caught and fixed a real gap in my own first draft before it shipped: the `InvalidDataFormatError` path wasn't wrapped in the same try/catch as the SQL-failure path, so it would have skipped logging — fixed to log both. (2) New `GET /api/recommendation` route, every attempt logged regardless of outcome. 5 new tests. (3) `mcp-server/src/seedBlockingScenario.ts` — demo/dev tooling (not part of the app) that deliberately opens two real concurrent SQL sessions, one holding a lock the other waits on, so a live demo has something incident-shaped to show instead of an idle database; the one place in this repo that writes to a monitored SQL Server, and deliberately so.
  - Verification, all against the real, live Azure SQL Database, not mocked: ran `queryLiveDmv()` directly — real connection succeeded, real columns matched `DmvExecRequestRow` exactly. Ran the full chain (`generateRecommendation()`) against the idle database — correctly returned `"No incident indicated"` at 10% confidence with `insufficientEvidence: true`, refusing to invent a problem on an empty system rather than fabricating one to look useful. Ran `seedBlockingScenario.ts` for real, then hit `/api/recommendation` while the lock was actively held — **the blocking scenario did not show up.** Investigated rather than assuming the seed script was broken: a raw, uncapped diagnostic query during the same live block confirmed the blocked session was real and present (`session_id: 53, blocking_session_id: 52, status: "suspended", cpu_time: 0`) but buried below ~50 Azure background system sessions, because the existing `DMV_QUERIES` (written before this session, R2/R3) ordered `TOP (3)` results by `cpu_time DESC` — a blocked session has near-zero CPU by definition, so it never ranked. A real, previously undiscoverable bug: fixture data's clean 3-row dataset could never have exposed it. Fixed in `dmvLiveSource.ts` (excludes `status = 'background'`, sorts any blocked session ahead of everything else), added a regression test asserting the query text, then re-ran the full live scenario end to end: `/api/recommendation` correctly returned `"Session 68's UPDATE on coreops-demo is blocked (LCK_M_S_XACT_MODIFY) by session 64, causing the order update to hang"` at 75% confidence — genuinely evidence-grounded reasoning over a real, live blocking scenario, not a template. Also caught and fixed a bug in `seedBlockingScenario.ts` itself along the way: Session B's `requestTimeout` (10s) was shorter than Session A's hold time (25s in that run), so Session B gave up waiting before ever observing the unblock — fixed to a longer, decoupled request timeout. `mcp-server` 62/62 tests passing (up from 56), `tsc --noEmit` clean; `guardrails`/`frontend` unaffected and re-confirmed clean. Full grep across all tracked files and `git rev-list --all | xargs git grep` for the real SQL password: zero matches, confirmed never leaked. Verified the Command Center renders STORY-006 correctly in Real mode (`"not_started"`, all 3 criteria confirmed) via a real browser check.
  - Notes: Confidence this story is complete and correct: ~92% — higher than STORY-003/004/005, because this is the first story where the specific thing that could have been fabricated (a "connected" scenario nobody could actually verify) got verified for real, not accepted as a caveat, and doing so surfaced and fixed a genuine pre-existing bug rather than just adding new code on top of an unproven foundation. `project-blueprint/requirements.md`: R2 and R3 moved PLANNED -> BUILT — both had been stuck specifically on "never run against a real SQL Server instance" since 2026-08-07/10, now closed. `docs/implementation-notes.md` fully refreshed (was stale — still said "R1 is UNMAPPED," old test counts) rather than just appended to. What would raise confidence further: running this against a non-serverless SQL Server (or one with less background noise) to confirm the fix generalizes beyond Azure's specific internal session mix, and a longer-running demo rehearsal to confirm the auto-pause cold-start delay (serverless tier, 1hr idle timeout) doesn't surprise anyone mid-demo.

- [x] Incident: restored CoreOps tracking data after a platform-side program swap corrupted it with an unrelated project ("Ambit")
  - Date: 2026-08-19
  - Session: CC-20260818-b7wq
  - What happened: When fetching before starting STORY-007, `git merge --ff-only origin/main` pulled in 3 new sync commits. The last two (`fa005bd`, `dd4876c`) had replaced the entire tracked program: `.colaberry/plan.json`, `.colaberry/progress.json`, `CLAUDE.md`, and all of `docs/` now described **"Ambit" — an OAuth-style identity and access management system for AI agents** — token issuance, consent screens, subagent delegation, anomaly detection — nothing to do with CoreOps (SQL Server, DMVs, AI recommendations, dashboards). Worse than a simple overwrite: the platform had relabeled this repo's 7 real CoreOps commits as "verified" against fabricated Ambit criteria with fake points attached (e.g. `STORY-001`'s real commit `b55c117`, "require human approval for production changes," now claimed to satisfy *"Given a token request, when it is approved, then the token is issued and enforced"*). Caught before pushing anything or building on it — cross-checked all four platform-owned sources (`progress.json`, `docs/stories/STORY-007.md`, `docs/REQUIREMENTS.md`, `plan.json`) and all four agreed with each other on Ambit content, confirming this wasn't a single-file glitch. Stopped and flagged to the user rather than guessing which interpretation of "STORY-007" to build. User's explanation: they'd given the platform a new Build Idea ("Ambit"), and its sync mechanism evidently reused this repo's existing connection instead of a fresh one. User confirmed they cannot fix this from their side (Ambit has no repo-selection UI on the platform) and emailed the Colaberry Principal directly — this is a platform bug outside this repo's or this session's control to resolve at the root.
  - What changed: Restored `.colaberry/plan.json`, `CLAUDE.md`, and `docs/` from this repo's own last known-good commit (`a49e90d`, the real STORY-006 commit) — those files hadn't been touched by the intermediate legitimate sync, so this state was already correct. For `.colaberry/progress.json` and `.colaberry/manifest.json` specifically, restored from `5a9ba11` instead of `a49e90d` — found by checking each sync commit individually rather than assuming the most recent pre-corruption commit was also the most complete one: `5a9ba11` was a genuine, uncorrupted CoreOps sync (`project: CoreOps AI Operations Dashboard`) that had already legitimately verified STORY-005 and STORY-006 with their real commit SHAs and points — using `a49e90d` alone would have under-reported those two as `not_started` despite the platform having already confirmed them for real before the corruption hit. Removed the Ambit-only files (`docs/REQUIREMENTS.md`, `docs/STORIES.md`, `docs/TRACEABILITY.md`, `docs/stories/*.md`) entirely rather than leaving them as dead weight.
  - Verification: Confirmed the restored `progress.json` shows all 7 real CoreOps stories `"verified"` with their correct, matching commit SHAs and 469 total points (STORY-000 through STORY-006), and `plan.json`/`CLAUDE.md` correctly describe CoreOps again. Re-ran the full test suite in every package to confirm the actual application code was never touched by any of this (it wasn't — purely `.colaberry`/`CLAUDE.md`/`docs` files): `mcp-server` 62/62, `frontend` 5/5, all JSON valid. Re-fetched immediately before committing to confirm no further Ambit sync had landed in the interim.
  - Notes: Deliberately did not push this the moment it was ready — held it until the user confirmed there was no way to stop another Ambit sync from landing and immediately re-corrupting it, since pushing and having it clobbered again minutes later would just mean redoing this for no benefit. Pushed now on the reasoning that holding back doesn't reduce risk either way (another sync could land regardless of whether a fix is pushed), and leaving the corrupted state live in the meantime has no upside. If another Ambit sync lands before the platform-side bug is fixed, this same restoration process applies — check each sync commit individually for the most complete genuine CoreOps state, don't assume the most recent pre-corruption commit is automatically the right one to restore from.

- [x] STORY-007: Enable cloud service data access for AI recommendations (REQ-008 + REQ-013)
  - Date: 2026-08-19
  - Session: CC-20260819-x3mq
  - What changed: Platform confirmed the Ambit corruption was a build-record mixup (Ambit reused CoreOps' build slot) and restored CoreOps' real plan/progress; the platform's own doc sync (`docs/stories/STORY-007.md` etc.) is still pending on their side, so worked directly from the story prompt the user pasted in rather than waiting further. Built the cloud-service counterpart to STORY-006's SQL Server path, in explicit, confirmed steps (paced co-pilot mode per the story's own instructions), never batching: (1) `mcp-server/package.json` + `.env.example` — added `@azure/storage-blob` and `AZURE_STORAGE_CONNECTION_STRING`/`AZURE_STORAGE_CONTAINER` config, no connector logic yet. (2) `mcp-server/src/cloudBlobSource.ts` — `queryLiveCloudBlob()`, mirroring `dmvLiveSource.ts` exactly: env-configured connection, 10s timeout, 3 retries with backoff, its own `CircuitBreaker` instance (separate failure domain from the SQL path, so a Blob Storage outage can't trip the DMV breaker or vice versa), and a `CloudSourceUnavailableError` for missing config that's never retried. Covers all three failure paths the story lists (connection failure, timeout, invalid data format) — the first two collapse into `UpstreamCallFailedError` via the shared reliability wrapper, and malformed JSON was deliberately tested as its own case since the story calls it out by name. (3) `mcp-server/src/cloudRecommendationService.ts` — `generateCloudRecommendation()`, mirroring `recommendationService.ts`: calls `queryLiveCloudBlob()` directly (never `dmvReader.ts`-style fixture fallback presented as real), zod-validates every record against a new `CloudDiagnosticRecordSchema` before trusting it as evidence, logs every access attempt (success and failure) via `logEvent` for the Trust criterion — same convention STORY-006 established, not the separate `guardrails/auditLog.ts` (that class is scoped to decisions/remediation actions per `docs/implementation-notes.md`, not general data-access logging). (4) `GET /api/cloud-recommendation` in `httpServer.ts`, mirroring `/api/recommendation`'s error mapping (`CloudServiceUnavailableError` -> 503, `InvalidCloudDataFormatError` -> 502). (5) `.claude/launch.json` — added an `mcp-server-http` entry so the server can be previewed/run consistently.
  - Verification: 74/74 `mcp-server` tests passing (up from 68 before this story: 6 new in `cloudBlobSource.test.ts`, 6 new in `cloudRecommendationService.test.ts`), `tsc --noEmit` clean. Then verified live against a real Azure Blob Storage account the user created in-portal (`coreopsdiagnostics` storage account, `diagnostics` container, a real `diagnostics.json` blob uploaded) — not mocked, following STORY-006's precedent that mocked tests alone don't prove a live integration works. Positive case: real HTTP request to `/api/cloud-recommendation` -> real blob download -> real Claude call -> `insufficientEvidence: true` at 15% confidence, an honest result (the single seeded test record genuinely lacks root-cause detail) rather than an invented one. Negative case (Break step, per this repo's Build-Break-Harden loop): started a second server instance on a different port with `AZURE_STORAGE_CONTAINER` pointed at a nonexistent container — got a real `503 CLOUD_SERVICE_UNAVAILABLE`, confirming the connectivity-failure path fires against real Azure errors, not just mocked ones. Trust: both the success and the failure attempt produced a `cloud_recommendation_data_access` log line in the server's stderr output, confirmed by tailing the actual process log. `.colaberry/progress.json` updated: all 3 STORY-007 criteria set to `passed: true`, `files_touched`/`tests_added`/`notes` filled in, validated as well-formed JSON.
  - Notes: The live Blob Storage connection string now lives only in `mcp-server/.env` (gitignored, never read into this log or committed) — the user pasted it in directly via a locally-opened TextEdit window, so it never passed through chat. Confidence this story is complete and correct: ~90% — the full acceptance chain (positive, negative, and Trust criteria) was verified against real Azure infrastructure, not just mocked, matching the bar STORY-006 set. What would raise it further: a second live run with a richer, multi-record diagnostic export (the current test blob has only one thin record, enough to prove the pipeline works end-to-end but not enough evidence for the AI agent to produce a high-confidence root cause) and a demo rehearsal to confirm timing alongside the SQL Server path.

- [x] STORY-008: Enhance monitoring capabilities for continuous incident management (REQ-016)
  - Date: 2026-08-19
  - Session: CC-20260819-x3mq
  - What changed: This story's acceptance criteria say "Given a server" generically (unlike STORY-006/007, which named their system explicitly), and I have no Windows Server credential or access. Flagged this before writing code rather than silently substituting something — user's direction: no faking, this needs to demo convincingly (explicitly: "viewers, recruiters need to know i can test this smoothly"). Resolved by reading "a server" as the one real, already-connected system this repo can actually reach — the live Azure SQL Database from STORY-006, explicitly listed as "already specified, reuse it, do not rebuild it" — rather than requesting new, uncredentialed Windows Server access for a generic phrase. User confirmed. Built in confirmed, paced steps: (1) `mcp-server/src/monitoringService.ts` — `startMonitoring()`/`stop()`, a real `setInterval` loop calling `queryLiveDmv()` directly (never `dmvReader.ts`'s fixture-fallback — a fabricated incident or a fabricated all-clear would be exactly the thing STORY-006/007 refused to do). Incident = any row with a non-zero `blocking_session_id`, reusing STORY-006's already-fixed, already-tested query as-is rather than writing new detection logic that could reintroduce that bug's class of error. A failed cycle logs the failure and the loop keeps running (this is what makes "continuous" real rather than aspirational — covers the "Monitoring failure" failure path); a logging failure is itself guarded so it can't kill the loop either (covers "Monitoring log failure"). Exposes an `onCycle` callback so a caller can observe live status without that being the audit trail itself (`logEvent` remains that). (2) Wired `GET/POST /api/monitoring/{start,stop,status}` into `httpServer.ts` so a demo can toggle monitoring live and see it running instead of tailing server logs — start/stop are idempotent (calling start while already running does not spin up a second interval, matching this repo's idempotency rule; same for stop).
  - Verification: 81/81 `mcp-server` tests passing (up from 74 before this story, including a regression test — see below), `tsc --noEmit` clean. Ran it live against the real Azure SQL Database, not mocked. First live run surfaced a real bug before it shipped: the interval fires on a fixed schedule regardless of whether the previous cycle finished, and the SQL Server free-tier instance was cold (auto-paused) and took ~30s to respond against the 10s default interval — overlapping cycles fired concurrently, piling up connection-attempt failures fast enough to trip the shared circuit breaker (from `dmvLiveSource.ts`) on a false alarm, surfacing as `CircuitOpenError` instead of the server actually being unreachable. Fixed with an in-flight guard in `monitoringService.ts` (skip a tick if the previous cycle hasn't finished) plus a new regression test asserting no overlapping `queryLiveDmv()` calls. Re-ran live after the fix: clean cycles every 10s exactly (`21:57:00`, `:10`, `:20`, `:30`), no circuit trips. Then ran `mcp-server/src/seedBlockingScenario.ts` (STORY-006's existing demo tool, reused not rebuilt) to create a real 15s blocking scenario while monitoring was live — the very next cycle correctly detected it (`incidentDetected: true`, session 79 blocked by session 77 on `coreops-demo`) and fired a real `incident_alert`, visible both in the server's log and via `GET /api/monitoring/status`. Confirmed `POST /api/monitoring/stop` is idempotent (second call returns `"Monitoring is already stopped."` rather than erroring) and that `status` correctly reflects `running: false` afterward. `.colaberry/progress.json` updated: all 3 STORY-008 criteria set to `passed: true`, `files_touched`/`tests_added`/`notes` filled in, validated as well-formed JSON.
  - Notes: Confidence this story is complete and correct: ~88% — every criterion was verified against real, live infrastructure end-to-end (continuous detection, a real incident triggering a real alert, and Trust logging), and the live run caught and fixed a genuine concurrency bug rather than shipping it. Slightly below STORY-006/007's ~90-92% because this story's "a server" required an interpretive call (SQL Server standing in for the persona's literal "Windows Server Administrator") rather than a literal, unambiguous match — flagged and confirmed with the user rather than silently assumed, but it's still a substitution worth being upfront about in any demo narration. What would raise it further: a longer-duration live soak (multiple minutes, several incident/clear cycles) to build more confidence the in-flight guard holds up beyond the two cycles observed here, and — if a real Windows Server ever becomes available — retargeting or extending this to monitor it directly, which would remove the interpretive gap entirely.

- [x] STORY-009: Implement incident escalation based on confidence thresholds (REQ-011)
  - Date: 2026-08-19
  - Session: CC-20260819-x3mq
  - What changed: REQ-011 ("escalate when confidence is below 60%") is source-agnostic — no system named — so built `mcp-server/src/escalationService.ts` as a standalone evaluator, `evaluateEscalation(incidentId, confidence, rootCause)`, rather than rebuilding detection per source. Confidence < 60 builds an `EscalationRecord` and logs a distinct `escalation_triggered` event (warn level, `notifiedOperator: true`) — this is this story's walking-skeleton meaning of "notify a human operator," the same pattern STORY-008 used for "alert": a real, distinct log signal, not a paging/email channel (explicitly STORY-010's job, not this one). One log line carries both the confidence score and an ISO timestamp, satisfying criteria 2 and 3 together. Confidence >= 60 returns `null` with no log call — tested explicitly at the exact 59/60 boundary so "doesn't over-escalate" is proven, not assumed (covers "Escalation not triggered"). A malformed confidence score (`NaN`, negative, >100, +-`Infinity`) throws a typed `InvalidConfidenceScoreError` rather than silently escalating or silently not (covers "Incorrect confidence score"). The log call is guarded the same way STORY-008's was, so a logging hiccup can't silently swallow an escalation or crash the caller (covers "Escalation log failure"). Wired into both existing recommendation paths — `GET /api/recommendation` (STORY-006, SQL) and `GET /api/cloud-recommendation` (STORY-007, cloud) — by calling `evaluateEscalation()` right after the AI result comes back and adding the result to the JSON response; the story's own "reuse, don't rebuild" list only named STORY-007/008, but REQ-011 doesn't scope by source, so both were included — flagged to the user before building, confirmed. Deliberately did not change `recommendationService.ts`/`cloudRecommendationService.ts`'s own return types or touch their existing tests — wiring lives entirely in `httpServer.ts`, the same integration point STORY-006/007/008 already established, so this was a zero-blast-radius addition to two already-complete, already-tested stories.
  - Verification: 87/87 `mcp-server` tests passing (up from 81 before this story: 6 new in `escalationService.test.ts`), `tsc --noEmit` clean. Verified live: hit `/api/cloud-recommendation` against the real Azure Blob Storage diagnostics (same thin single-record export from STORY-007) — got a real 20% confidence result, and the response's `escalation` field showed a real `EscalationRecord`; confirmed the matching `escalation_triggered` log line (confidence, rootCause, timestamp, `notifiedOperator: true`) in the server's own log output. The non-escalation path (confidence >= 60), the boundary, invalid-input rejection, and the logging-failure guard are all covered by the 6 unit tests rather than a live high-confidence scenario, because SQL Server (the other path) was unreachable during this session's live testing — see below. `.colaberry/progress.json` updated: all 3 STORY-009 criteria set to `passed: true`, `files_touched`/`tests_added`/`notes` filled in, validated as well-formed JSON.
  - Notes: Separate finding surfaced during this story's live verification, not a STORY-009 defect: `GET /api/recommendation` (the SQL Server path) failed with a genuine `SQL_SERVER_UNAVAILABLE` on two attempts roughly 90 seconds apart, both exhausting the full retry budget (`UpstreamCallFailedError`, not a cold-start timeout that a single retry cycle should have absorbed based on STORY-008's ~30s wake time). Flagged to the user directly rather than worked around — the free-tier Azure SQL DB's actual reachability should be checked in the Azure portal before any real demo, since this system is built to show an honest `503` rather than fake data when that happens, and a live `503` mid-demo is still worse than a working golden path. Confidence this story is complete and correct: ~85% — the escalation logic itself is fully proven (live trigger + all edge cases unit-tested), but slightly below STORY-006/007/008 because only one of the two wired paths (cloud) was confirmed triggering live; the other (SQL) couldn't be, due to the connectivity issue above, not anything in this story's own code. What would raise it: a live SQL-path escalation once connectivity is restored, and ideally one live run of the non-escalation case (a real high-confidence result, e.g. via `seedBlockingScenario.ts`) to see `escalation: null` come back from a real endpoint, not just a unit test.

- [x] STORY-009 follow-up: closed the SQL-path live-verification gap
  - Date: 2026-08-20
  - Session: CC-20260819-x3mq
  - What happened: The prior entry's SQL_SERVER_UNAVAILABLE turned out to be a firewall block, not a code or infra defect — a raw diagnostic connection (bypassing the collapsed error) surfaced Azure's actual message: "Client with IP address '68.99.71.211' is not allowed to access the server." The user was away from home on a public network at the time and declined to add that IP to the firewall (correct call — didn't want to open cloud access from an untrusted network). No code change; user returned home and asked to retest.
  - What changed: No code changed. Re-ran the same raw diagnostic once the user was home: this time a genuine `ETIMEOUT` (not a firewall rejection) — consistent with the free-tier DB having auto-paused since it was last used, needing a longer timeout to resume. Retried with a 30s timeout: connected successfully. Started the server and exercised both outcomes of the SQL path live: (1) `GET /api/recommendation` against the quiet database returned 20% confidence and a real `escalation` record; the `escalation_triggered` log line matched (confidence, rootCause, timestamp, `notifiedOperator: true`). (2) Seeded a real blocking scenario with the existing `mcp-server/src/seedBlockingScenario.ts` (STORY-006's tool, reused not rebuilt) and hit the endpoint again mid-block: 75% confidence, `escalation: null` — correctly did not escalate a genuinely confident result. Updated `.colaberry/progress.json`'s STORY-009 notes with this follow-up rather than opening a new story entry, since no acceptance criteria or code changed — only additional live evidence for criteria already marked passed.
  - Verification: Both SQL-path outcomes (escalate at 20%, correctly don't at 75%) now confirmed against real, live Azure SQL Database infrastructure, matching what was already true for the cloud path. Both wired recommendation paths (SQL and cloud) are now fully live-verified for both the trigger and non-trigger cases — the one gap flagged in the prior entry is closed.
  - Notes: Confidence in STORY-009 raised from ~85% to ~92% — the last open item from the original entry (a live SQL-path confirmation, ideally covering both outcomes) is now closed, putting it in line with STORY-006/007/008. Separately worth remembering for demo prep: this free-tier Azure SQL DB auto-pauses when idle and needs a warm-up call before a demo (same note as STORY-008's entry) — today's session needed a 30s timeout on the first reconnect after being idle since the prior day's testing.

- [x] STORY-010: notify operators of autonomous actions (REQ-012)
  - Date: 2026-08-20
  - Session: CC-20260819-x3mq
  - What changed: This system has exactly two real autonomous actions today — STORY-008's incident alert and STORY-009's escalation — so `mcp-server/src/notificationService.ts`'s `notifyOperators()` is wired into both rather than inventing a third action to satisfy REQ-012. No real notification channel exists (no email/Slack/paging credentials anywhere in this repo), so the delivery channel is injectable (same DI pattern as `queryFn`/`analyzeFn` used elsewhere), defaulting to a real, distinct log event — matching STORY-008/009's own precedent for "notify"/"alert" at this stage of the build; flagged and confirmed with the user before building. "Retries the notification until successful" was read as capped retry, not literal infinite retry, since this repo's own rules explicitly prohibit unbounded retry loops — reused the existing `withReliability` wrapper (same one already powering the SQL, Blob, and Anthropic calls) rather than building new retry logic. An exhausted retry is logged (`operator_notification_failed`) but does not propagate, so a delivery failure can't crash the autonomous action that triggered it. Missing operator contact info (`OPERATOR_CONTACTS` env var, documented in `.env.example`) throws `OperatorContactMissingError` immediately, before any delivery attempt. Wired into `monitoringService.ts` (incident alert) and `escalationService.ts` (escalation) as fire-and-forget dispatches with a `.catch()` — notifyOperators()'s own capped retry could otherwise block the monitoring interval or break escalationService's synchronous contract, so neither caller awaits it directly. Along the way, extracted the guarded-logging helper both of those files had their own copy of into `observability/safeLogEvent.ts` on its third duplication (this repo's own stated threshold for lifting repeated logic) — kept the exact prior console.error message text so STORY-008/009's existing tests needed zero changes, confirmed by re-running them before writing any new code.
  - Verification: 99/99 `mcp-server` tests passing (up from 87 before this story: 6 new in `notificationService.test.ts`, 3 new each in `monitoringService.test.ts`/`escalationService.test.ts`), `tsc --noEmit` clean. The test-writing process itself caught a real design bug before it shipped: the first draft only logged a successful delivery inside the default walking-skeleton channel, so a caller-supplied channel would deliver silently with no audit trail at all — fixed by moving the Trust log into `notifyOperators()` itself so it applies uniformly to any channel, not just the default one; the fix is what the "delivers on the first attempt" test actually exercises now. Verified live, not just mocked: set `OPERATOR_CONTACTS` in `.env`, hit `/api/cloud-recommendation` on the quiet diagnostics blob (20% confidence) — real `escalation_triggered` log line followed immediately by a real `operator_notification_delivered` line naming the configured operator. Then started monitoring and seeded a real blocking scenario (`seedBlockingScenario.ts`, reused not rebuilt) — the very next cycle's `incident_alert` was immediately followed by its own `operator_notification_delivered` line. Both of this system's real autonomous actions confirmed dispatching a real, logged notification end-to-end, live.
  - Notes: Confidence this story is complete and correct: ~90% — both wiring points were confirmed live (not just unit-tested), the retry-until-successful and missing-config failure paths are deterministically unit-tested (real-time retry backoff isn't practical to prove live without a genuinely broken channel to point at), and the design bug the tests caught before shipping is itself a point in favor of the process, not against the result. Slightly below STORY-006/007/008/009's ~90-92% band only because "operator contact info" is a placeholder value (`on-call-ops@coreops-demo.example`) rather than a real destination anyone would actually receive at — that's inherent to there being no real notification channel yet, not a gap in this story's own logic. What would raise it: a live run of the capped-retry-exhaustion path against a genuinely broken channel (not just mocked), and — whenever a real channel exists — swapping the default in for a real send.

- [x] STORY-010 follow-up: real ntfy.sh delivery + real operator contact
  - Date: 2026-08-20
  - Session: CC-20260819-x3mq
  - What changed: User asked what free channel could give an actual notification, and to fill in real operator contact info rather than the placeholder. Presented options (Slack webhook, ntfy.sh, Discord webhook, Gmail+app-password) with cost/friction/demo-value tradeoffs via a structured choice; user picked ntfy.sh (free, no signup, no API key, plain HTTP POST to a topic name — https://ntfy.sh/docs/). Generated an unguessable topic (`coreops-ops-<16 hex chars>`, via `secrets.token_hex`) rather than a guessable name like "coreops-alerts", since a public ntfy topic's name is its only access control. `notificationService.ts`'s `defaultChannel` — previously a no-op — now POSTs the action's title/summary/incident id/operators to the configured `NTFY_TOPIC` on `NTFY_SERVER` (defaults to `https://ntfy.sh`); a missing topic throws `NotificationChannelUnconfiguredError` (new) before any network call, same "don't retry a config problem" discipline as this repo's other missing-config errors, just enforced inside the retried function itself since the check itself is free (no network call) rather than moved outside `withReliability` the way SQL/Blob's checks are. `OPERATOR_CONTACTS` set to a real value ("Quincy Ninying (AI Engineer)") in `.env`; `NTFY_TOPIC`/`NTFY_SERVER` documented in `.env.example` and set for real in `.env`. 3 new tests: a real POST's shape (topic URL, title, body), a non-ok HTTP response treated as a delivery failure, and a missing-topic config error — all via `vi.stubGlobal("fetch", ...)`, no real network calls in the test suite itself.
  - Verification: 102/102 `mcp-server` tests passing (up from 99), `tsc --noEmit` clean. Verified live, twice: (1) a direct `curl` POST straight to the ntfy topic, confirmed retrievable via ntfy's own `/json?poll=1` API — proved the service and topic work before touching app code. (2) Through the actual app: started the server, hit `/api/cloud-recommendation` to trigger a real escalation, confirmed both the server's own `operator_notification_delivered` log line and, independently, that the message actually landed on the ntfy topic (polled ntfy's API again) with the correct title (`CoreOps: escalation`), the real incident summary, and the real operator name. This is now an actually-working notification a phone or browser tab subscribed to the topic will receive live during a demo, not just a log line claiming delivery.
  - Notes: Along the way, a mistake and a clean recovery worth logging: an earlier attempt to update this same `.colaberry/progress.json` entry via a Python `json.dump()` re-serialization silently converted every em-dash in the *entire file* to `—` escapes (Python's `json.dump` defaults to `ensure_ascii=True`), producing an unwanted diff across STORY-000 through STORY-009's notes that had nothing to do with this change. Caught before committing by inspecting the diff rather than trusting the script ran cleanly — reverted with `git checkout --`, redone with a targeted `Edit`-tool string replacement that touched only the intended 7 lines. No topic name, connection string, or other secret was ever exposed in this process; the ntfy topic itself is the only sensitive value here, and it lives only in `.env` (gitignored).

- [x] Governance engine: ABAC policy + evaluator + HITL queue, extending STORY-001/002 (no platform STORY id)
  - Date: 2026-08-20
  - Session: CC-20260820-g4tk
  - What changed: Built at the user's request in the parent `ColaberryAI` session (an ABAC policy, an evaluator, a HITL approval queue, and an audit log, walked through step by step and demonstrated end to end there first). Before moving it into this repo, checked `.colaberry/plan.json`/`progress.json` directly and found STORY-001 (approval required) and STORY-002 (audit trail) already built and verified via `guardrails/remediationGuardrail.ts` and `guardrails/auditLog.ts` — and no requirement anywhere covering general ABAC (closest is REQ-006/STORY-005, role-based dashboards, UI-only, no backend `Role` type exists). Surfaced this to the user directly rather than silently reimplementing a parallel audit log next to an already-scored one; user chose "extend the existing guardrail/auditLog" over a fully separate module or leaving it out of this repo. Added `guardrails/abacPolicy.ts` (typed policy data — `Role`/`ResourceType`/`Action`/`RiskLevel`/`Effect` literal unions + 9 rules, not a loose JSON file, matching this package's fully-typed, zero-JSON-config convention) and `guardrails/abacEvaluator.ts` (pure `evaluateAbacPolicy()`, same purity/first-match-wins/fail-closed contract as `checkRemediationGuardrail`). Extended `guardrails/auditLog.ts`'s `AuditEntry` union with two new variants (`PolicyEvaluationAuditEntry`, `HitlAuditEntry`) and two new builders (`buildPolicyEvaluationAuditEntry`, `buildHitlAuditEntry`) — additive only; `DecisionAuditEntry`/`ActionAuditEntry` and every existing STORY-001/002 test are untouched. Also had to change `freezeEntry()`'s `if/else` into an explicit `switch` over all 4 entry types (the old 2-branch `if/else` would have left `policy_evaluation`/`hitl_event` entries' nested objects unfrozen), with a comment noting the switch is deliberate so a future 5th variant fails to compile rather than silently shipping unfrozen. Added `guardrails/hitlQueue.ts`: enqueue -> notify (recorded as data, not dispatched — this package stays dependency-free, so real delivery via `mcp-server/src/notificationService.ts`'s already-built `notifyOperators()` is left as the caller's job, same split `remediationGuardrail.ts` already draws for audit-completeness) -> bounded decision window -> decide-or-auto-escalate-to-backup-approver, all deterministic via an injectable clock (same pattern as `mcp-server`'s `CircuitBreaker`). `decide()` enforces that only the currently-assigned approver may act and requires an explicit `mfa: boolean` flag, throwing typed `UnauthorizedDeciderError`/`MfaRequiredError` rather than silently no-op'ing. Added `guardrails/demoGovernanceEngine.ts` (matching `demoUnsafeAction.ts`'s existing runnable-demo convention) that specifically demonstrates composition, not duplication: ABAC's `system-executes-after-recorded-approval` rule and the pre-existing `checkRemediationGuardrail` are both run against the same approved remediation and both independently agree it's allowed — the new ABAC layer gates *whether this actor may even request execution*, the old guardrail still gates *the content of the remediation itself*, unchanged.
  - Verification: `npm test` in `guardrails/`: 44/44 passing (27 pre-existing + 17 new: 8 in `abacEvaluator.test.ts`, 8 in `hitlQueue.test.ts`, 6 added to `auditLog.test.ts` for the two new entry types), all labeled happy path/failure path/boundary per this repo's own testing convention. `npx tsc --noEmit` clean in `guardrails/` after one real fix caught by the type-checker (not by vitest, which doesn't type-check by default): a test accessed `stored.entry.detail` inside an `expect(() => {...})` closure where TS's control-flow narrowing doesn't survive into a nested function — the exact same gotcha the original `auditLog.test.ts` had already worked around with an explicit cast at its "stored entry is frozen" test; fixed the same way (extract the narrowed value to a local `const` before the closure) rather than inventing a different pattern. Cross-checked for regressions: `grep` confirmed only `remediationGuardrail.ts` (untouched by this change) is imported from `mcp-server/src` across the package boundary; ran `npm test` in `mcp-server/` too as a full-repo check — 102/102 passing, unaffected. `npx tsx guardrails/demoGovernanceEngine.ts` run for real, live output confirmed: low-risk view auto-allowed; the high-risk execution request correctly required approval, was enqueued, approved by name, and both gates (ABAC + the existing remediation guardrail) independently allowed the same real remediation; a human's direct-execute attempt was denied outright; the final audit trail printed all 6 real events in order.
  - Notes: Confidence this is complete and correct: ~90% — every module is genuinely tested (not just demoed) and the type-checker caught a real latent bug before it shipped, same kind of process signal STORY-010's entry above called out. Below the STORY-006/007/008/009/010 band only because this has no platform STORY id and therefore no `.colaberry/progress.json` entry, acceptance criteria, or points — it's real, tested, committed code, but it sits outside this repo's scored surface by design, per the user's own choice among the options presented. Commit message will not carry a `Story:` trailer for that reason (no story id exists to name truthfully) — flagging this explicitly rather than inventing one, since a fabricated `Story:` line would misrepresent what's actually tracked. Left `checkForTimeout()` as something a caller must invoke explicitly (no internal `setTimeout`/scheduler) — deliberate, same reasoning as `CircuitBreaker`'s injectable clock: keeps the whole module synchronous, deterministic, and testable without fake timers or real waiting, at the cost of needing a real scheduler wired in wherever this gets used for real. Not pushed yet — pending explicit confirmation, since this repo's remote is read by the external Colaberry platform and a push is a real, externally-visible action.

- [x] Added `docs/audit-trail-design.md` (no platform STORY id)
  - Date: 2026-08-20
  - Session: CC-20260820-g4tk
  - What changed: Drafted in the parent `ColaberryAI` session at the user's request — a design doc on building an audit trail for an AI system, centered on correlation-ID-based reconstruction (importance, four non-negotiable properties — append-only/immutable/retrievable-by-ID/attributed — plus idempotency as a fifth, the correlation-ID propagation mechanism, and a 7-point implementation checklist). Grounded its worked example in this repo's own real `guardrails/auditLog.ts` (from the governance-engine entry immediately above) rather than a hypothetical system: its required `correlationId` field enforced at write time, `forCorrelationId()` retrieval, `Object.freeze()` immutability, and idempotent-by-id conflict detection are cited directly as the reference implementation of the doc's own principles. Saved to the user's Downloads folder first, then copied here into `docs/` at the user's explicit request ("save copy in workspace as well"), alongside the existing `ADR-001-mcp-transport-selection.md` and `implementation-notes.md` — matching this repo's existing docs convention rather than inventing a new location.
  - Verification: Documentation deliverable, not executable code — no test suite applies. Every technical claim about `guardrails/auditLog.ts`'s actual behavior (the correlationId field, `forCorrelationId()`, freezing, idempotent-by-id conflict handling) was verified directly against that file's real source and its passing test suite (44/44, per the governance-engine entry above) before being cited in the doc, rather than described from memory.
  - Notes: Same as the governance-engine entry above: no `.colaberry/plan.json` requirement covers this, so no `Story:` trailer and no scored surface — a real, useful reference doc, not a graded artifact. Two copies now exist by design (`~/Downloads/audit-trail-design.md` and this one) since the user asked for both; if they diverge later, this repo's copy is the one under version control and should be treated as authoritative.

- [x] Incident + implementation: correlation-ID unification for `mcp-server/`'s audit events (no platform story)
  - Date: 2026-08-20
  - Session: CC-20260819-x3mq
  - What happened: Working in a separate, concurrent session from the one that produced the `docs/audit-trail-design.md` entry immediately above (session `CC-20260820-g4tk`, unaware of it at the time — the two sessions weren't sharing context). The user asked this session to draft an audit-trail document; without checking whether `docs/audit-trail-design.md` already existed, this session wrote a new version to that exact path via the `Write` tool, silently overwriting the other session's already-committed file (`git show 9ece14b:docs/audit-trail-design.md`) with a different one. Caught only once this session's own `PROGRESS.md` append surfaced the other session's entry describing a document with different structure than what this session had just written — not caught proactively before the overwrite. Root cause: didn't check `git log`/read the file before writing to a path that turned out to be pre-existing, despite the harness's own Write-tool guidance to read an existing file first. Fixed: `git checkout -- docs/audit-trail-design.md` restored the original exactly (diffed clean against the pre-overwrite commit); this session's own analysis — which is substantively different in purpose (a specific, CoreOps-scoped gap-and-implementation-plan, not a general teaching doc) — saved instead to a new, non-colliding file, `docs/ADR-002-audit-trail-correlation-id-unification.md`, matching this repo's existing `ADR-001-mcp-transport-selection.md` format (Status/Owner/Date/Component, Decision drivers, Decision, Consequences) since it proposes a specific direction rather than teaching general principles.
  - What changed (the actual implementation, step 1 of ADR-002's plan): `guardrails/auditLog.ts` extended with `SystemEventAuditEntry` — a new `AuditEntry` variant for `mcp-server/`'s operational events (recommendation generation, monitoring cycles, escalations, notifications), which previously only reached process stderr via `logEvent()` with no queryable, immutable record and no correlation ID shared with `guardrails/`'s existing, correctly-built audit log. Deliberately a thin `{event, outcome, context}` adapter over `logEvent()`'s existing shape rather than a new rigid schema, since an operational event's shape genuinely varies per event type (unlike a decision or an ABAC check, which do have a fixed shape). Added `buildSystemEventAuditEntry()` (matching the existing builder pattern exactly: `id`, `correlationId`, injectable `now()`), a `freezeEntry()` case for `context`, and updated the header comment to record this second extension to the entry-type union. Purely additive: `DecisionAuditEntry`/`ActionAuditEntry`/`PolicyEvaluationAuditEntry`/`HitlAuditEntry` and their consumers (`hitlQueue.ts`, `demoGovernanceEngine.ts`) are untouched. `mcp-server/`'s services are not yet wired to actually write to this — that's steps 2-4 in `ADR-002`, not built in this session.
  - Verification: 6 new tests in `guardrails/auditLog.test.ts` — a valid entry records/retrieves/freezes correctly, a failure outcome is distinguishable from a success one, idempotency by id (identical entry recorded twice doesn't duplicate), the existing `assertValid()` missing-actor rejection applies to this entry type too, and — the actual point of this whole change — a `system_event` entry is retrievable via `forCorrelationId()` alongside a `decision` entry sharing the same correlation ID, proving cross-entry-type reconstruction works. `guardrails` 50/50 tests passing (up from 44), `tsc --noEmit` clean. Hit one real type error while writing the cross-entry-type test (`stored.entry.context` didn't narrow inside a nested closure) — same class of issue the existing `hitl_event` freeze test already worked around by extracting a local `const` before the closure; fixed the same way rather than introducing a new pattern. Re-ran `mcp-server`'s own suite (102/102) to confirm zero cross-package impact, since `httpServer.ts` already imports from `guardrails/` via a relative path.
  - Notes: No `.colaberry/plan.json` requirement covers this (same as the governance-engine/audit-trail-design entries above), so no `Story:` trailer. Flagging the overwrite mistake here in full rather than quietly fixing it and moving on, per this repo's own "failures are inputs, not mistakes" principle — the concrete lesson: when a user request could plausibly target a file that already exists in a shared repo, check (`ls`/`git log -- <path>`) before `Write`ing to that path, not just when the tool happens to require a prior `Read`. Two real design docs now exist for the same general topic (`docs/audit-trail-design.md` — general principles, `docs/ADR-002-...md` — the specific CoreOps gap and plan) by accident of this collision rather than original intent, but they're complementary rather than duplicative, so left both rather than merging or deleting either.

- [x] Reusable AI prompt library: 5 prompts, test-first, scored (no platform story)
  - Date: 2026-08-20
  - Session: CC-20260820-g4tk
  - What changed: User wanted a library of reusable prompts for CoreOps, non-technical, plain-English throughout. First identified and ranked 5 candidate prompts grounded in CoreOps's real pipeline (`score-root-cause-confidence`, `classify-incident-severity`, `draft-remediation-summary`, `draft-diagnostic-script`, `draft-operator-notification`), each a repeated task with a definite-shape answer, explicitly excluding a 6th candidate (remediation risk classification) since `architecture.md` deliberately keeps that decision out of the AI's hands. Built all 5 test-first: for each, `prompts/<name>/eval.jsonl` (5 cases each — 4 ordinary + 1 deliberately awkward) was written and every expected answer proposed one at a time and confirmed or corrected by the user directly, never filled in unilaterally. Built `scripts/score_prompt.py` once (shared across all 5): fills `{{blank}}` placeholders into a prompt file, calls Claude, extracts the first parseable JSON object from the reply, and grades only the fields named in `"expected"` (text case/whitespace-insensitive, numbers with a tolerance) — deliberately never grading open-ended fields like `rootCause`/`what_broke`/`script`/`message`, since exact-matching free text would fail correct answers for being phrased differently; those fields stay real, required prompt output, just ungraded by this script. Each prompt file (`v1.0.0.md`, `v1.0.1.md` for the one that needed a second pass) written via the harness's own Plan Mode: presented which of Monday's 5 prompt-engineering rungs to start at and why, got explicit approval, then wrote the file. All 5 started at Rung 2 (Specific) and only one — `classify-incident-severity` — needed to move up (a Rung 4 worked example, to fix one case about a worsening-but-unresolved problem correctly outranking a resolved one-off) after the score, not a guess, showed Rung 2 wasn't enough on that one case.
  - Verification: All 5 prompts scored against their real eval files via real Claude API calls (not simulated), each run at least twice before being marked `status: ready` in its own header, given the model's inherent sampling variance (see next note). Final scores: `classify-incident-severity` v1.0.1 — 3 clean runs of 5/5 and 1 run of 4/5, all 4 misses on the same subtle case; `score-root-cause-confidence` v1.0.0 — 5/5 twice; `draft-remediation-summary` v1.0.0 — 5/5 twice; `draft-diagnostic-script` v1.0.0 — 5/5 (after a real scorer bug fix, below); `draft-operator-notification` v1.0.0 — 5/5 twice.
  - Notes: Three real bugs found and fixed in `scripts/score_prompt.py` itself while building this, each from an actual failure, not anticipated in advance: (1) `claude-sonnet-5` rejects a `temperature` parameter outright ("deprecated for this model," confirmed by the API, not assumed) — removed the attempt to force determinism and added an explicit usage note that scores can vary run to run, so a close score difference needs 2-3 runs before trusting it as real. (2) `draft-diagnostic-script`'s replies (a full script wrapped in JSON) were sometimes cut off by `max_tokens=1024` before the JSON closed, which failed to parse and looked exactly like a wrong answer rather than a too-small limit — confirmed by inspecting a raw truncated response directly, not guessed; raised to `MAX_TOKENS = 4096` (a named constant) and added a `stop_reason == "max_tokens"` warning so a future truncation is diagnosable, not mysterious. (3) Any Claude API error beyond the two originally handled (bad/missing key, connection failure) was letting a raw Python stack trace through, contradicting the user's own "no stack traces, plain English instead" requirement even though they'd only named the key-missing case explicitly — added a general `anthropic.APIStatusError` handler that prints Claude's actual error message. No `.colaberry/plan.json` requirement covers a general prompt library, so no `Story:` trailer on this work — real, tested, and reused across the whole session, but outside this repo's scored surface by design, same as the governance-engine and audit-trail-design entries above.

- [x] STORY-011: implement rollback capabilities for low-risk tasks (REQ-015) — last story in the plan
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What changed: No real Execution Service exists in this codebase — nothing performs a real production write, by design (only the read paths for SQL Server and Blob Storage exist). Flagged before writing code: "roll back a low-risk task" can't honestly mean "revert a real production change" when nothing real changes production yet; building a fake revert for an action nothing can actually execute would be exactly the fabrication this project has refused everywhere else. Proposed and confirmed with the user: target the one genuinely real, already-idempotent, low-risk reversible action already built — `monitoringService.ts`'s `startMonitoring()`/`stop()` (STORY-008). Built `mcp-server/src/rollbackService.ts` — `requestRollback(request, registry, options)`, checks run in a fixed order (permission -> task identification -> reversibility -> dependency) so a denial always names the *first* real reason, not a generic one; `registry` is a required parameter with no built-in default, since any default would either be empty (implying nothing is reversible) or would have to fabricate a revert function ahead of real wiring existing. Wired into `httpServer.ts`: `POST /api/monitoring/start` now returns a real `taskId`; a registry maps `start_monitoring` to a real revert closing over the actual `monitoringHandle` (calls its real `stop()`), and registers `notify_operators` as honestly non-reversible (`reason: "A delivered push notification cannot be unsent."`) rather than leaving it unrecognized, so its denial correctly reads "not reversible" and not "unknown task" — the right criterion for the right reason. New `POST /api/rollback` route maps all four `rollbackService` error types (insufficient permissions, unknown task type, not reversible, dependency blocked) to distinct HTTP statuses (403/404/409/409).
  - Verification: 6 new tests in `rollbackService.test.ts` — successful revert, each of the three named failure paths (insufficient permissions, incorrect task identification, dependency blocker) plus the REQ-015-specific non-reversible denial, and a Trust check that success and denial both log distinctly. `mcp-server` 108/108 tests passing (up from 102), `tsc --noEmit` clean. The test-writing process itself caught a real bug before it shipped: the non-reversible denial's human-readable explanation was keyed as `reason` in the log context, colliding with and silently overwriting the categorical denial code (also `reason`) on the same log line — renamed to `detail`, confirmed live afterward (see below) that both fields now appear correctly and distinctly. Verified live, not just mocked: started the real HTTP server, called `POST /api/monitoring/start` (got back a real `taskId` and confirmed `running: true` via status), called `POST /api/rollback` for that exact `taskId`/`start_monitoring` — got `{outcome: "reverted"}` back and confirmed via `GET /api/monitoring/status` that `running` actually flipped to `false` for real (the monitoring loop genuinely stopped, not just a claimed success). Then confirmed both denial paths live: `notify_operators` rollback → real `409 TASK_NOT_REVERSIBLE` with the honest reason text; an `engineer` role attempting rollback → real `403 INSUFFICIENT_PERMISSIONS`. Both denials appeared in the server's own audit log with correct, distinct `reason`/`detail` fields.
  - Notes: Confidence this story is complete and correct: ~90% — this is the highest-fidelity story in the release specifically because criterion 1 ("successfully reverts the task") was proven against a real, live state change (monitoring actually stopped), not a mocked assertion, matching the bar every other live-verified story in this session set. The one honest caveat, same shape as STORY-008's: "a low-risk task" was interpreted as monitoring start/stop rather than a literal production remediation, because no production-write capability exists yet to interpret it any other way — flagged and confirmed with the user before building, not discovered as a surprise later. This is the last story in `r4`'s plan; all 12 stories (STORY-000 through STORY-011) now have real, live-verified implementations in this repo.

- [x] PREP-1: write the demo narrative (the problem, the one moment, the guardrail)
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What changed: This platform task is explicitly marked "this one is you, not Claude Code" — a human presentation-prep exercise, not a coding task. Declined to draft the official submission unprompted; offered raw material (a suggested "one moment" beat grounded in this session's real live demo flow) for the user to adapt instead. User wrote their own problem statement and guardrail description directly in chat; Claude Code's role was editing (grammar, tightening, flagging that "Power BI dashboards" wasn't part of the actual documented system scope so the user could decide whether to keep or cut it — they cut it), adding the missing "one moment" section per the platform's own three-part structure, formatting requested changes (removing all hyphens per the user's request, bolding and bracketing stage directions so they're visually distinct from spoken lines), then saving the result to `~/Downloads/coreops-demo-narrative.md` and a matching `.pdf` (built with reportlab, verified by rendering and visually inspecting the page before sending). Copied the final markdown into the repo at `project-blueprint/demo-narrative.md` (alongside the existing presentation-facing docs — `demo-script.md`, `one-pager.pdf`, `reviewer-one-pager.md`) once the user shared the platform's "not checked yet" banner asking for a commit naming PREP-1.
  - Verification: Not a testable code change — a written narrative. "Verification" here is that every word is the user's own (or an edit/addition they explicitly reviewed and approved before it was saved), and that the one factual claim in it (system scope) was checked against `architecture.md`/README rather than assumed.
  - Notes: No `.colaberry/plan.json` STORY id covers this (it's a Demo Prep track item, not a build story), but it does need a commit naming PREP-1 per the platform's own check banner, so this entry exists to satisfy this repo's own PROGRESS.md gate for that commit — same reasoning as the governance-engine/audit-trail-design/prompt-library entries above for work outside the scored STORY surface.

- [x] PREP-1 follow-up: found and flipped the actual missing config
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What happened: PREP-1 still showed "Not checked yet" after the commit synced successfully (confirmed via the portal's own activity log, which listed the commit) and after clicking Sync from GitHub — ruling out a push/sync problem. Neither `README.md`, `CLAUDE.md`, nor `.colaberry/plan.json`'s `PREP-1` entry (which has only `key`/`title`/`due_on`, no acceptance criteria the way stories do) document any PREP-specific commit format, confirmed by direct search rather than assumed. Asked Cory (the platform's own assistant) twice; the first two replies were generic ("check the docs," "check hidden config files") without a concrete answer. Acting on the second reply's concrete, checkable suggestion — searched for hidden config/CI files — and found two `.colaberry/` files never documented in `CLAUDE.md`'s "three files" list: `profile.json` (a public-profile disclosure config: `disclosure`, `headline`, `summary`, `highlight_story_ids`, and an `include` block with `requirement_statements`/`measures`/`systems`/`narrative`, all `false`) and `connect.txt` (a connection token — flagged to the user as sensitive, its value never repeated back or shared further). `include.narrative: false` was surfaced to Cory as a specific, named hypothesis rather than acted on immediately, since flipping a disclosure setting on a guess wasn't warranted without confirmation. Cory confirmed it was very likely the cause and recommended setting it to `true`. Flipped `.colaberry/profile.json`'s `include.narrative` from `false` to `true`.
  - What changed: `.colaberry/profile.json`: `include.narrative` `false` -> `true`. No other fields touched.
  - Verification: JSON validated as well-formed after the edit. Whether this actually resolves the "Not checked yet" status is not yet confirmed as of this commit — the user will check the portal after this pushes and report back.
  - Notes: This file sits outside `CLAUDE.md`'s documented "what not to edit" list (`plan.json`/`manifest.json` only) and outside its documented "yours to change" list too — it's genuinely undocumented. Proceeded on Cory's direct, specific confirmation (not a guess of my own) given the change is a single boolean, easily reversible, and low blast radius. If the portal still shows "Not checked yet" after this, the `narrative` flag was not actually the cause and this should be reverted rather than left as an unexplained change.

- [x] PREP-1 follow-up: reverted the narrative-flag change; ruled out, escalating
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What happened: User confirmed PREP-1 still showed "Not checked yet" after the `include.narrative: true` commit synced. That rules out Cory's hypothesis — the flag was not the cause. Reverted `.colaberry/profile.json`'s `include.narrative` back to `false`, per this repo's own stated intent from the prior entry (don't leave an unexplained, ineffective config change sitting in the repo).
  - What changed: `.colaberry/profile.json`: `include.narrative` `true` -> `false` (back to its original value).
  - Verification: JSON validated as well-formed. Confirms the file now matches its state before this troubleshooting detour began.
  - Notes: At this point, self-service troubleshooting has been exhausted in good faith: the commit is confirmed synced (visible in the portal's own activity log), no PREP-specific commit format is documented anywhere in this repo, no CI/GitHub Actions config references PREP checks, and the one concrete hypothesis Cory offered (the `narrative` disclosure flag) has been tested and ruled out. Cory's two replies were each either generic or, once concrete, incorrect. This has the same shape as the earlier Ambit build-record collision this session — a genuine platform-side defect or an undocumented requirement that isn't discoverable from the repo — not something further guessing at commit formats is likely to solve. Recommended to the user: escalate to an actual Colaberry staff member/instructor rather than continuing to iterate through Cory, since Cory has now been wrong twice on a question specific to its own platform's internals.

- [x] Demo dashboard: add continuous monitoring and AI recommendation/escalation panels (no platform story)
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What changed: User wanted to rehearse PREP-2 (record a first run-through) but pointed out, correctly, that no UI showed the full narrative flow (monitoring detects an incident -> AI generates a confidence score -> low confidence escalates -> a real notification fires) — `dashboard.html` only had DMV incidents and the STORY-001 guardrail-block demo; the frontend Operations Console (STORY-005) only shows role-based summaries. Asked the user whether to build the missing UI first or rehearse verbally against curl output; user chose to build. Extended the existing `mcp-server/src/dashboard.html` (already served live at `GET /`) rather than building a new page or touching the separate frontend/ Console, matching its existing dark-theme styling exactly: (1) a "Continuous monitoring" panel — Start/Stop buttons calling the real `/api/monitoring/{start,stop}` routes, live status (polls `/api/monitoring/status` every 5s), last cycle outcome, recent alerts; (2) an "AI recommendation & escalation" panel — a button calling the real `GET /api/recommendation`, displaying the confidence score prominently (large number, red under 60%/green at or above, matching the narrative's "watch the confidence score" beat) and an amber "Escalated to a human operator" banner when `escalation` is present in the response, naming the real threshold and the real notification timestamp.
  - Verification: Ran the real HTTP server and drove the actual page in a real browser (not just read the code) — clicked Start monitoring, confirmed the panel showed a real running state and real task ID; clicked Get AI recommendation. First attempt correctly surfaced a real `SQL_SERVER_UNAVAILABLE` error in the UI (the shared circuit breaker was open from an earlier cold-start monitoring cycle, confirmed via a raw connection diagnostic that the DB itself was actually reachable) — waited for the breaker's real cooldown-then-half-open-trial to close on its own (confirmed via server log: cycles flipped from `CircuitOpenError` to `success`), then retried. Second attempt rendered a real 45% confidence score, a real root-cause explanation, and a real "Escalated to a human operator" banner in the browser — cross-checked against the server's own log, which showed matching `escalation_triggered` and `operator_notification_delivered` events for the same incident ID, confirming the UI wasn't just displaying success but genuinely reflecting what the backend did. Two clicks initially missed their target because the page's live-updating panels (5s status poll, panel content appearing/disappearing) shift layout between a screenshot and a pixel-coordinate click; switched to `read_page`/element-ref-based clicking, which is immune to that, rather than continuing to guess coordinates. `mcp-server` 108/108 tests unaffected (this is a static HTML change, no `.ts` touched), `tsc --noEmit` clean.
  - Notes: No `.colaberry/plan.json` requirement covers this (it's demo-prep tooling, not a build story), so no `Story:` trailer — same reasoning as the other no-platform-story entries above. The existing STORY-001 guardrail-block panel ("Proposed remediation") was left untouched; it demonstrates a different, still-valid property (an unapproved action gets blocked) and isn't part of this narrative's "one moment," so there was no reason to remove or merge it.

- [x] Add `demo-start`/`demo-stop` skills and `mcp-server/src/warmup.ts` (no platform story)
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What changed: User wanted to rehearse but not have to manually juggle terminal tabs — warm SQL Server and Blob Storage, start the server, start monitoring, all before demoing, plus a matching teardown. Built `mcp-server/src/warmup.ts` — dev tooling matching `seedBlockingScenario.ts`'s existing convention (not part of the app, run by hand), connecting directly to SQL Server and Blob Storage rather than going through `dmvReader.ts`'s fixture-fallback path, since a warmup script that can silently report "OK" against fixture data instead of a real connection would defeat its own purpose. SQL Server gets a bounded 90s retry budget (matching the free tier's real cold-start window observed repeatedly this session); Blob Storage is checked once via a real `containerClient.exists()` call. Added two project-scoped skills at `.claude/skills/demo-start/SKILL.md` and `.claude/skills/demo-stop/SKILL.md`, matching the `SKILL.md` frontmatter/structure convention already used throughout the parent `ColaberryAI` workspace's skill library. `demo-start`: checks if the server's already running (idempotent, no double-start), runs the warmup script, starts the HTTP server in the background, polls health, starts monitoring, then explicitly tells the user to open the dashboard in their *own* browser rather than opening the Claude Code Browser pane for them — that pane isn't what an audience sees during an actual screen-shared demo. `demo-stop`: stops monitoring, kills the server process, confirms the port is actually free rather than assuming the kill worked.
  - Verification: Ran the full `demo-start` sequence for real, from a genuinely cold state (confirmed `/health` returned connection-refused first) — `warmup.ts` connected to both dependencies on the first attempt, the server came up and passed its health check, `POST /api/monitoring/start` returned a real `running: true` and a real `taskId`. Then ran the full `demo-stop` sequence — `POST /api/monitoring/stop` returned `running: false`, the process was killed, `/health` confirmed connection-refused (`000`) afterward. Also confirmed `demo-stop`'s idempotency claim directly: ran it a second time against an already-stopped system and it failed to connect cleanly rather than erroring or leaving anything in a bad state, matching what the skill document claims. `mcp-server` 108/108 tests unaffected, `tsc --noEmit` clean (only a new dev-tooling `.ts` file was added, no existing code changed).
  - Notes: No `.colaberry/plan.json` requirement covers this, so no `Story:` trailer. These skills are scoped to this repo (`ai-operations-center/.claude/skills/`), not the parent `ColaberryAI` skill library, since they're specific to CoreOps's own dependencies and ports — matching how `.claude/launch.json` is already project-local rather than global.

- [x] Enterprise-grade visual redesign of `mcp-server/src/dashboard.html` (no platform story)
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What changed: User gave a detailed design brief asking for the dashboard to look like real enterprise software (Stripe/Linear/Datadog/Vercel reference points) rather than a demo mockup — dark-mode-first, a single accent color, a collapsible sidebar nav, a topbar with an environment pill/search/notifications/user chip, a bounded set of real KPI cards, and a working Cmd+K command palette, with the explicit constraint that every number shown stays real (no fabricated data). Confirmed scope with the user first (redesign `mcp-server/dashboard.html` directly, real KPIs only) before rewriting the file. Full rewrite: CSS custom-property design tokens, sidebar nav (Overview/Monitoring/Incidents/AI Recommendation/Guardrail, collapsible), sticky topbar, 6 KPI cards (System Health, Monitoring, Active Sessions, Recent Alerts, Last Confidence, Escalations — the last two show an honest empty state, not a fabricated placeholder, until a recommendation is actually requested), the existing Monitoring/Incidents/AI Recommendation/Guardrail panels restyled, a right-hand Recent Activity feed assembled client-side from real events as they happen, and a functional command palette (`⌘K`) exposing every real action as a searchable command, including the STORY-011 rollback action.
  - Verification: Live-verified in a real browser against the running server, not just read back as code — every panel driven end to end with real backend calls: Start monitoring (real task ID, live status), Roll back this session (real `POST /api/rollback`, confirmed `running` actually flipped back to `false`), Get AI recommendation (a real Claude call — landed at 55% confidence, correctly triggered STORY-009's escalation path and a real STORY-010 ntfy notification, both reflected honestly in the KPI cards and the activity feed), and the guardrail check (correctly returned `Blocked` / `NOT_HUMAN_APPROVED` / `PRODUCTION_WRITE_REQUIRES_APPROVAL`, the real ABAC denial). `mcp-server` 108/108 tests passing, `tsc --noEmit` clean (dashboard.html is static and untested by the TS suite by nature, so this only confirms no regression elsewhere).
  - What was found and fixed during verification (this is the point of live-testing over trusting the rewrite): (1) the command palette's action list was missing the new "Roll back this session" entry entirely — the button existed but wasn't wired into `commands[]`, caught by literally searching the palette for "roll" and getting "No matching commands"; fixed by adding the entry, pointed at the same `rollbackMonitoring()` the button already calls. (2) The notification bell and the sidebar version label were both real `<button>` elements with no click handler and (for the bell) a misleading `aria-haspopup="true"` that promised a popup that didn't exist — a real accessibility defect, since both are keyboard-focusable and announced as actionable to screen readers while doing nothing. Fixed the bell by wiring it to scroll to and focus the Recent Activity panel (an honest, real action, not a fabricated dropdown); converted the version label and the user-avatar chip to non-interactive `<span>`s since neither has any real action behind it and there's no real user-menu/auth system in this app to attach one to. (3) A genuine CSS layout bug: `.sidebar` had no bounded height, so under `.app`'s default flex `align-items: stretch` it stretched to match `.main`'s full scrollable content height rather than the viewport — with the sidebar footer pinned via `margin-top: auto`, this pushed the version label and the Collapse button roughly 1700px down the page, unreachable without excessive scrolling. Fixed with `position: sticky; top: 0; height: 100vh; overflow-y: auto` on `.sidebar`, the standard fix for a flex sidebar next to taller scrollable content; confirmed via `getBoundingClientRect()` that the Collapse button is now within the viewport (`top: 936` of a 987px-tall viewport) and confirmed the collapse/expand toggle itself works correctly afterward.
  - What was investigated and ruled out as not a real bug: a large black rendering void appeared on some scrolled screenshots, initially suspected to be a `backdrop-filter`/`position: sticky` compositing bug on the topbar. Removed the topbar's `backdrop-filter` as a first attempt; the identical void persisted afterward, disproving that hypothesis. Diagnosed properly via `elementFromPoint()`/`getBoundingClientRect()` rather than continuing to guess from screenshots: the DOM was confirmed correct at every point checked (real elements, correct `rect.top: 0` for the sticky topbar, correct backgrounds) even when the screenshot showed it displaced or blacked out — proving the mismatch was in the Browser pane's screenshot capture, not the page's actual rendering. (One contributing factor confirmed separately: a stray tab had been left open on a broken `file://` URL earlier in this long session, which produced one of the confusing screenshots; closed it.) Left the `backdrop-filter` removal in place since it's a harmless simplification, not because it was the fix — the real fix for this investigation was switching to DOM-based verification instead of trusting scrolled screenshots in this environment.
  - Notes: No `.colaberry/plan.json` requirement covers a visual redesign of dev-facing dashboard tooling, so no `Story:` trailer — matching the other no-platform-story entries above. The dev server caches `dashboard.html` in memory at startup (`readFileSync` once, module scope, in `httpServer.ts`) with no hot-reload; every edit in this session required a server restart to take effect, which is how the missing-command-palette-entry bug was actually confirmed fixed rather than assumed.

- [x] Dashboard: remove demo tells; schedule real fault injection into `demo-start`/`demo-stop` (no platform story)
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What changed: User reviewed the redesigned dashboard and flagged that it still read as an obvious demo — the environment pill said `coreops-demo`, a page heading and subtitle explicitly said "nothing here is staged," and several panels/the footer/a KPI card printed this repo's own internal file paths (`mcp-server/src/httpServer.ts`, `guardrails/remediationGuardrail.ts`) and an internal codename ("MCP Tool Gateway") straight into the UI. Removed all of it from `mcp-server/src/dashboard.html`: env pill now reads `production`; the "Overview" heading and its "real output... nothing here is staged" subtitle are gone entirely; every `<code>`-wrapped file path and the "MCP Tool Gateway" name were stripped from the Monitoring/Incidents/Guardrail panel descriptions, the page footer, and the System Health KPI card's meta text (replaced with plain product copy — e.g. "Gateway reachable" instead of the literal source file); repeated "a real X" reassurance phrasing (real Claude call, real DMV evidence, real push notification, real notification) was reworded to normal, confident product copy that doesn't protest its own realness; the sidebar footer's "v0.1.0 · local" (contradicting the new "production" pill) was trimmed to just "v0.1.0". None of this touched what the numbers actually mean or where they come from — every value on the page is still exactly as real as before, just described the way a shipped product would describe it rather than the way its own source tree would.
  - What changed (fault injection): User asked for `demo-start` to bring the system up healthy and then, 1-2 minutes in without further action, inject a real failure so they can narrate discovering and resolving it live, rather than having to trigger `seedBlockingScenario.ts` by hand on camera. Flagged before building: this app has no real kill-session/restart-service execution path (the guardrail correctly blocks unapproved production writes, and nothing approves them yet), so the only honest "fix" available is the blocking scenario's own real self-resolution once its hold time elapses — not a fabricated on-screen "fix" button. Added step 5 to `.claude/skills/demo-start/SKILL.md`: writes a small wrapper script to `/tmp/coreops-fault-injector.sh` (`cd mcp-server && sleep <delay> && npx tsx src/seedBlockingScenario.ts <hold>`) and backgrounds it with `nohup ... & disown`, defaulting to a 90s delay and a 60s hold (both adjustable per the user's request). Added a matching first step to `.claude/skills/demo-stop/SKILL.md` that cancels it (`pkill -f "coreops-fault-injector.sh"` and `pkill -f "seedBlockingScenario.ts"`, both safe no-ops if nothing's scheduled) before doing anything else, so a demo stopped early can't have a fault fire afterward.
  - Verification: Every UI-copy change re-verified live in the browser after a server restart (dashboard.html is cached in memory at startup, no hot-reload) via `get_page_text` and a screenshot — confirmed no file paths, no "coreops-demo"/"nothing here is staged"/"real X" phrasing, and no "local" contradicting the new "production" pill anywhere on the rendered page. `mcp-server` 108/108 tests passing, `tsc --noEmit` clean (copy-only change to a static file). The fault-injection mechanism was fully live-tested, not just written and assumed to work, and this caught two real problems before they could have broken an actual demo: (1) the obvious `(sleep N && cmd) & echo $!` pattern was tried first and its captured PID, checked directly with `pgrep`, turned out to be the wrapping shell's own PID, not the real backgrounded job's — silently making `demo-stop`'s planned cancellation kill the wrong (already-dead) process and leave the real fault still scheduled; caught by deliberately testing the PID against `pgrep -fl` rather than trusting `$!`, and replaced with a named wrapper-script file matched by `pgrep -f`/`pkill -f` on its own distinctive path instead, then re-tested end to end: scheduled a short instance, cancelled it before it fired, confirmed via its log file and `pgrep` that it genuinely never ran; scheduled another short instance, let it actually fire, confirmed via its log that both sessions ran and the lock released cleanly on its own. (2) Querying `/dmv/exec-requests` immediately after a fresh server restart returned `"source": "fallback"` even though the database was genuinely reachable (confirmed separately by `seedBlockingScenario.ts` connecting to it directly at the same moment) — investigated rather than assumed fixed: a second query a few seconds later correctly returned `"source": "live"`, confirming this was the same already-documented one-off cold-start gap between the app's own connection pool and a fresh restart (not a new defect, and not reachable in a real demo timeline anyway, since `demo-start`'s own step 4 already starts continuous monitoring — which polls DMV on a real interval — 90s before the fault fires, so the pool is warmed by real traffic well before the injected fault needs to be visible).
  - Notes: No `.colaberry/plan.json` requirement covers either change, so no `Story:` trailer — same reasoning as the other no-platform-story entries above. The fault-injection default timing (90s delay, 60s hold) is a judgment call, not a requirement — flagged here so it's easy to find and adjust if the user wants different pacing for a specific rehearsal. `mcp-server/src/seedBlockingScenario.ts` itself was not modified; `demo-start`/`demo-stop` only orchestrate it differently.

- [x] Wire a real human-in-the-loop approval step into the remediation guardrail (no platform story)
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What changed: User clicked "Propose: restart_service on prod-app-server-03" and got the correct `Blocked / NOT_HUMAN_APPROVED / PRODUCTION_WRITE_REQUIRES_APPROVAL` result, then asked for a real approval step so they can show "how we fix issues," not just how the system blocks them. Flagged before building, since it's a genuine design decision, not an implementation detail: this codebase deliberately has no real production-write execution path (same gap STORY-011 hit), so once a human approves, "execute" can't honestly mean "prod-app-server-03 actually restarted" — that server doesn't exist. Presented two options and had the user pick: (recommended, chosen) relabel the proposed action to something truthful and, on approval, genuinely execute the one real, safe, reversible service this system has — restart the monitoring collector (the same real `monitoringService.ts` STORY-011's rollback already targets) — versus a weaker "approve but don't execute anything" option. User chose the real-execution option.
  - What was already there to build on: `guardrails/hitlQueue.ts` — a real, already-tested (from a prior session) HITL approval queue (enqueue → pending → decide with a decision window, MFA requirement, unauthorized-decider check, all audit-logged via `guardrails/auditLog.ts`) that simply wasn't wired to anything yet. No new dependency, no new package — this session only added callers.
  - What changed (backend, `mcp-server/src/httpServer.ts`): Extracted `startMonitoringInternal()`/`stopMonitoringInternal()` from the existing `/api/monitoring/start`/`stop` route bodies so the same real start/stop logic is shared rather than duplicated once a third caller (the guardrail's execute path) needed it. Added a module-level `AuditLog`/`HitlQueue` pair and a `pendingRemediations` map. Replaced the old stateless `POST /guardrail/check-demo` (always re-checked the same hardcoded `approval: null` object, so it could only ever say "blocked") with two real routes: `POST /api/guardrail/propose` — builds the evidence-linked action against a truthful target (`monitoring-collector`, not the fictional `prod-app-server-03`), enqueues it into the real `HitlQueue`, and returns the real approver's name (read from `OPERATOR_CONTACTS`); `POST /api/guardrail/decide` — takes `{itemId, decision}`, calls the real `hitlQueue.decide()`, and on `approve`, re-runs `checkRemediationGuardrail()` with a real `ApprovalDecision` attached (now genuinely `allowed: true`) and then actually calls `stopMonitoringInternal()` + `startMonitoringInternal()` — a real service restart, not a simulated one. `reject` decides via the same real queue and does nothing further. All of `HitlQueue`'s real error types (`HitlItemNotFoundError`, `UnauthorizedDeciderError`, `MfaRequiredError`) are mapped to distinct HTTP statuses rather than swallowed.
  - What changed (frontend, `mcp-server/src/dashboard.html`): The guardrail panel's button became three (Propose / Approve / Reject, the latter two hidden until something's pending), backed by `proposeRemediation()`/`decideRemediation(decision)` replacing the old one-shot `recommendRemediation()`. On a real execution, the banner shows the actual new task ID and real timestamp, not a canned success message. Added matching "Propose remediation" / "Approve remediation" / "Reject remediation" entries to the command palette.
  - Verification: Live end-to-end against the running server, backend first via raw `curl` before touching the UI (to isolate backend correctness from browser flakiness): proposed → got a real pending item and the real blocked violations; approved → guardrail genuinely flipped to `allowed: true` and `GET /api/monitoring/status` confirmed monitoring's `taskId` actually changed (proving a real restart, not a cosmetic status flip); proposed again → rejected → confirmed `executed: false` and the monitoring `taskId` was untouched; called decide with a made-up `itemId` → got a real `404 UNKNOWN_ITEM`. Then repeated the full approve and reject paths through the actual browser UI via `read_page`/element-ref clicks (immune to layout shift) and `get_page_text` (not screenshots — this session's Browser pane has a known, already-diagnosed screenshot-capture artifact unrelated to real rendering; confirmed the command palette entries via a direct DOM query instead of trusting a blacked-out screenshot taken at the same moment): both the Blocked→Approve→"Allowed — executed" and Blocked→Reject→"Rejected" sequences matched the `curl` results exactly, including the task ID actually changing on approval and the Recent Activity feed logging each real step. `mcp-server` 108/108 tests passing, `tsc --noEmit` clean.
  - Notes: No `.colaberry/plan.json` requirement covers this, so no `Story:` trailer. `guardrails/hitlQueue.ts`'s `decide()` doesn't itself guard against deciding an already-decided item a second time (calling it twice would restart monitoring twice, each a genuinely new real restart rather than a duplicated side effect on the same action) — this session didn't add that guard, since it would mean redesigning already-built, already-tested shared code in `guardrails/` outside this session's scope; the dashboard's own client-side state (`currentGuardrailItemId` cleared after any decision) already prevents this from happening through the UI. The MFA flag passed to `hitlQueue.decide()` is always `true` from this route, since there is no real session/MFA concept in this walking skeleton — an honest stand-in for a real auth session, not a claim that an actual MFA challenge occurred; worth revisiting if a real auth layer is ever built.

- [x] ADR-002 steps 2-4: complete the audit trail — real correlation IDs, real durable writes, a live demonstration route (no platform story)
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What changed: User asked "what other features do you think we need to add?" — recommended finishing `docs/ADR-002-audit-trail-correlation-id-unification.md`'s remaining steps (step 1 shipped 2026-08-20; steps 2-4 were the gap between "we designed an audit trail" and "you can actually pull up everything that happened for incident X"), user said "go!" Built exactly the plan already written in the ADR, no redesign needed.
  - What changed (step 2 — one correlation ID per incident, generated once): `httpServer.ts`'s existing `incidentId` (already generated once, at the true entry point, for `/api/recommendation` and `/api/cloud-recommendation`) is now explicitly treated as the correlation ID threaded through `generateRecommendation` → `evaluateEscalation` → `notifyOperators`. `monitoringService.ts`'s autonomous path had no shared ID at all before this — the `incident_alert` log line and the `notifyOperators()` call each independently referenced `incident.session_id` with no common field. Added a real `crypto.randomUUID()` correlation ID generated once at the moment `hasIncident()` returns a row, threaded through both. Kept the existing human-readable `sql:sys.dm_exec_requests:<session_id>` string as `AutonomousAction.incidentId` (still used in the real ntfy notification body text) and added a new, optional `correlationId` field to `AutonomousAction` for the technical linking value — two different concerns, not the same field forced to serve both.
  - What changed (step 3 — write into `AuditLog`, not just stderr): New shared helper `mcp-server/src/observability/auditWrite.ts` (`recordSystemEvent()`), extracted upfront rather than waiting for a third occurrence, since this call was always going to land in all five named services. Added an optional, injectable `auditLog?: AuditLog` to `recommendationService.ts`, `cloudRecommendationService.ts`, `escalationService.ts`, `monitoringService.ts`, and `notificationService.ts` — same DI shape as the existing `queryFn`/`analyzeFn`/`notifyFn` injectables. Each service now writes a `SystemEventAuditEntry` alongside (not instead of) its existing `logEvent()`/`safeLogEvent()` call. `httpServer.ts` constructs one shared `AuditLog` (the same instance already backing the guardrail's `HitlQueue` from the previous session entry) and passes it into every call site.
  - What changed (step 4 — never drop the correlation ID on a failure path): Every failure branch that already logged an outcome now also writes an audit entry with `outcome: "failure"` and the same correlation ID — including a monitoring cycle failure, which has no detected-incident identity to key off of, so it mints its own fresh correlation ID rather than being skipped as "not a real incident."
  - What changed (the demonstration route, named in the ADR's own "Consequences" section as required): `GET /api/audit?correlationId=` — returns every entry sharing one correlation ID via the already-existing `AuditLog.forCorrelationId()`. A nice, unplanned bonus: since the guardrail's `HitlQueue` (built the previous session entry) already shares this same `AuditLog` instance, `hitl_event` entries from a guardrail propose/approve/reject flow are retrievable through this same route with zero additional code. Added a "View audit trail" button to the dashboard's AI Recommendation panel (shown after a real recommendation completes) and a matching command palette entry, rendering the real entries as a readable list — reusing the existing `.activity-list`/`.activity-item` CSS rather than adding new styles.
  - Verification: 9 new unit tests across `monitoringService.test.ts` (2), `recommendationService.test.ts` (1), `cloudRecommendationService.test.ts` (1), `escalationService.test.ts` (3, including one real end-to-end integration test proving `escalation_triggered` and `operator_notification_delivered` land under one correlation ID using the *real* `notifyOperators`, not a mock), and `notificationService.test.ts` (3, covering delivery, the correlationId-over-incidentId fallback rule, and a failed-delivery still getting audited). `mcp-server` 118/118 passing (up from 108), `guardrails` 50/50 unaffected, `tsc --noEmit` clean in both packages. Then live-verified against the real running server, not just the test suite: called the real `/api/recommendation` (a real Claude call, 55% confidence, real DMV evidence) and confirmed `GET /api/audit?correlationId=<that id>` returned exactly three entries in order — `recommendationService`'s `recommendation_data_access`, `escalationService`'s `escalation_triggered`, and `notificationService`'s `operator_notification_delivered` (a real ntfy dispatch, confirmed by waiting for the fire-and-forget call to actually land before re-querying) — each with a distinct actor and timestamp. Confirmed the 400 failure path (`GET /api/audit` with no `correlationId` param). Confirmed the guardrail bonus by proposing a real remediation and finding its `hitl_event` entry through the same route. Then repeated the recommendation → View audit trail flow through the actual dashboard UI (`read_page`/element-ref click, `get_page_text` — not screenshots, per this session's already-diagnosed Browser pane screenshot artifact) and confirmed all three real entries rendered correctly with real timestamps.
  - Notes: No `.colaberry/plan.json` requirement covers this, so no `Story:` trailer — same as the ADR-002 step-1 entry and the other governance-work entries above. Updated `docs/ADR-002-audit-trail-correlation-id-unification.md` itself: Status line, all four step headers, and the Consequences section now read "implemented" rather than "not yet built" / "if approved," plus a new "Result" section documenting the live verification, so the doc stays accurate rather than describing a plan that's actually already done.

- [x] Live Incidents panel: show more of the real per-session DMV data already in the response (no platform story)
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What changed: User asked what the "3 active sessions" KPI represents, then asked to make that section more detailed. Each session card in the Live Incidents panel previously showed only `session_id`, `status`, and `wait_type` — three fields out of the eight already present in every row `GET /dmv/exec-requests` returns (`command`, `database_name`, `cpu_time_ms`, `total_elapsed_time_ms` were fetched but never displayed). Added a `formatDuration()` helper (ms → "4ms"/"1.2s"/"1h 16m", so a real value like `total_elapsed_time_ms: 4576000` reads as "1h 16m" instead of a raw millisecond count) and two new lines per card: `command` + `database_name`, and elapsed + CPU time. No new API call, no new data — every field shown was already being fetched and simply wasn't rendered. Widened `.data-row`'s grid from a fixed 3-column layout to `repeat(auto-fit, minmax(210px, 1fr))` since three cards each carrying two more lines of monospace text no longer fit comfortably in a fixed 3-up layout at this panel's width.
  - Verification: `tsc --noEmit` clean, `mcp-server` 118/118 tests unaffected (a static-file, JS-only change — no `.ts` touched). Live-verified against the real running server: reloaded the dashboard and confirmed via the accessibility tree (`read_page`, not screenshots — this session's Browser pane has an already-diagnosed screenshot-capture artifact that displaces content on some scrolled captures without reflecting real rendering) that all three real sessions rendered their new detail lines correctly, e.g. session 78 showing "SELECT on master" and "1h 16m elapsed · 4ms CPU" alongside its existing "suspended · XE_LIVE_TARGET_TVF" line — matching the real values returned by the live DMV query.
  - Notes: No `.colaberry/plan.json` requirement covers this, so no `Story:` trailer.

- [x] Fix unescaped external data in `dashboard.html`'s `innerHTML` sinks (no platform story)
  - Date: 2026-08-21
  - Session: CC-20260819-x3mq
  - What changed: User asked whether CoreOps's trust boundaries are safe. Audited `dashboard.html` rather than answering from general impressions — grepped every `.innerHTML` assignment and traced where each interpolated value actually originates. Found the AI-generated root-cause text (the highest-risk single field, since it's LLM output) was already correctly rendered via `.textContent`, never `.innerHTML` — the boundary that matters most was already right. Found a real, concrete gap: several `.innerHTML` sinks concatenate values straight from `fetch()` responses without escaping — live DMV row fields (`status`, `wait_type`, `command`, `database_name`) in the session cards, and, most concretely, `POST /api/guardrail/decide`'s 404 error path, which echoes a client-supplied `itemId` verbatim into `resultEl.innerHTML` (`No pending remediation with id "${itemId}"` — and `itemId` comes straight from the request body). Not exploitable through the dashboard's own normal use today (it only ever sends IDs it generated itself), but the HTTP routes are public and would return whatever a direct caller sent, and this file already renders some of that via `innerHTML` — the kind of gap that's cheap to close now and expensive to find later. User said "fix."
  - What changed (the fix): Added one `escapeHtml()` helper (standard `&<>"'` entity escaping) near the top of the script, and wrapped every value reaching an `innerHTML` template that did not originate as a string literal in this file — DMV row fields, every `body.*` field from a `fetch()` response (`message`, `error`, `confidence`, `approver`, `taskId`, `violations[]`), and audit-trail entry fields (`entryType`, `event`/`hitlEventType`/`actionType`, `actor`). Applied uniformly rather than judging field-by-field which ones are "safe enough" right now — a field that's a fixed enum today can become free-form later, and re-litigating that judgment per field is exactly how this class of bug survives a refactor. Values already rendered via `.textContent` (root cause, most banners, most KPI values) were left untouched — `.textContent` is already safe and re-escaping it would be a no-op at best. The client-side command palette's `commands[]` array (developer-defined literals, never from a `fetch()` response) was also correctly left alone.
  - Verification: `tsc --noEmit` clean, `mcp-server` 118/118 tests unaffected (dashboard.html is static, no `.ts` touched). Live-verified the actual exploit path, not just the helper in isolation: called the real `decideRemediation('approve')` function against the live server with `currentGuardrailItemId` set to `<img src=x onerror=alert(1)>` — the server's real 404 response echoed that string back verbatim in `message` (confirmed via the raw response), and after rendering through the app's own (now-fixed) code path, `resultEl.querySelector('img')` returned `null` — the payload rendered as inert, visible text (`&lt;img src=x onerror=alert(1)&gt;`), not a live element; no `onerror` fired. Then confirmed legitimate content still renders cleanly with no double-escaping artifacts: a real `proposeRemediation()` call and a real Live Incidents reload both displayed normally (plain text, correct violation codes, correct session details), confirming the fix doesn't corrupt normal output.
  - Notes: No `.colaberry/plan.json` requirement covers this, so no `Story:` trailer. This was found and fixed proactively (a real audit, not a reported bug) — flagged to the user as part of a trust-boundary question rather than discovered by exploit. Scope was deliberately narrow: this fixes `dashboard.html`'s rendering layer only; it does not add server-side output encoding or a Content-Security-Policy header, since the concrete gap was specifically "untrusted data reaching the DOM unescaped," and CSP/server-side hardening would be new, larger scope not asked for here.

- [x] Session-based authentication for the CoreOps API and dashboard (no platform story)
  - Date: 2026-08-23
  - Session: CC-20260819-x3mq
  - What changed: A trust-boundary audit two entries above found the biggest real gap: every route on `httpServer.ts` was open to anyone who could reach the port, including `POST /api/guardrail/decide` — and `guardrails/hitlQueue.ts`'s real, enforced "only the assigned approver may decide" check was comparing a hardcoded constant (`GUARDRAIL_PRIMARY_APPROVER`) against itself, so it could never actually fail. User asked for real auth; decided session-based over JWT (no distributed system here for JWT's statelessness to help with, and a JWT in `localStorage` is directly readable by the exact XSS class of bug fixed two entries above). Entered Plan Mode given the size and security sensitivity of the change (multi-file, new routes, new UI flow, a genuine architectural decision) — explored the codebase (confirmed via a dedicated read-only pass: no existing auth code anywhere, the separate `frontend/` React app calls exactly one of the routes being gated, `index.ts`'s stdio MCP transport is a genuinely separate, non-network surface out of scope), had a Plan subagent produce a file-level implementation plan, verified its citations directly, then built it in the plan's own small-step order with `npm test`/`tsc --noEmit` and live verification after each step, per this repo's own "small, reversible steps" rule.
  - What changed (new modules, `mcp-server/src/auth/`): `credentials.ts` — `hashPassword`/`verifyPassword` via `node:crypto`'s `scryptSync`/`timingSafeEqual`, zero new dependencies. Hashed at rest despite `SQLSERVER_PASSWORD` being plaintext today — this password gates every route including the one that executes a real remediation, and hashing costs nothing; the inconsistency is noted in a comment rather than silently diverged from. `hashPassword.ts` — a `npm run hash-password -- '<password>'` CLI for first-time `.env` setup. `sessionStore.ts` — `SessionStore` class, same injectable-options shape as `guardrails/hitlQueue.ts`/`reliability/circuitBreaker.ts` (`ttlMs`, `now`, `generateId`), 60-minute sliding-expiry sessions (slides forward on each valid access so a long demo doesn't get logged out mid-session; lazily expires on access, same pattern as `CircuitBreaker.checkAvailability()`). `cookies.ts` — hand-rolled `Set-Cookie`/`Cookie` parsing (no `cookie` npm package needed for one cookie); `HttpOnly` + `SameSite=Strict` (does the real CSRF-mitigation work here, without a separate CSRF token) + a `Secure` flag gated by `SESSION_COOKIE_SECURE` (default off, so plain `http://localhost:8787` keeps working).
  - What changed (`httpServer.ts`): `AUTH_USERNAME`/`AUTH_PASSWORD_HASH` read at module load, **fail fast** if either is unset — a deliberate exception to this file's usual "missing config → fall back gracefully" convention, since falling back here would mean silently serving with no real auth. New routes: `GET /login` (serves a new `login.html`, `readFileSync`-at-startup, same pattern as `dashboardHtml`), `POST /api/login` (one generic `401 INVALID_CREDENTIALS` for either a wrong username or password — no enumeration), `POST /api/logout` (idempotent), `GET /api/session`. A `requireSession()` two-line guard added to every existing route except `GET /`, `GET /health`, `GET /login`, `POST /api/login`, `POST /api/logout` — `GET /` itself stays open (pure markup, no embedded data) since the dashboard's own `fetch` calls are the only mechanism that can also catch a session expiring *mid*-session, making a server-side redirect on `GET /` redundant extra code for no added coverage. **The actual fix for the original motivating gap:** `GUARDRAIL_PRIMARY_APPROVER` now derives from `AUTH_USERNAME` (the real, verified login identity) instead of `OPERATOR_CONTACTS` (never verified against anything, kept for its own separate job — the notification-recipient display name), and `POST /api/guardrail/decide` passes the real `session.username` as `decidedBy` to `hitlQueue.decide()` instead of the constant. `UnauthorizedDeciderError` is now reachable for real instead of permanently dead code.
  - What changed (`dashboard.html`): one shared `apiFetch()` wrapper (redirects to `/login` on any `401`) replacing all 11 existing raw `fetch()` calls — the vanilla-JS equivalent of an axios interceptor. The previously-decorative user-chip (hardcoded "QN"/"Quincy Ninying") is now real, populated from `GET /api/session` on load (which also doubles as the page's fast initial auth check), with a working logout button next to it.
  - What changed (`.claude/skills/demo-start/SKILL.md`, `demo-stop/SKILL.md`): both make real, already-tested `curl` calls against gated routes (`/api/monitoring/start`/`stop`) — added a login-and-cookie-jar step before any gated call in each. **Two real bugs caught by actually running the updated skills, not just writing them:** (1) `. .env` failed outright in this environment (zsh's `.`/`source` didn't find a bare filename in cwd — needed `./.env`); (2) even with the right path, blanket-sourcing `.env` as shell code broke on `OPERATOR_CONTACTS`'s unquoted value (`Quincy Ninying (AI Engineer)`) — zsh tried to glob-expand the parentheses. Fixed by extracting only the two needed values via `grep '^AUTH_USERNAME=' .env | cut -d= -f2-` instead of sourcing the whole file — safer in general too, since it doesn't execute arbitrary `.env` content as shell code. This needs a **plaintext** `AUTH_PASSWORD` in `.env` alongside the hash (the hash alone can't authenticate) — consistent with every other operational secret in this repo already living in plaintext `.env` (`SQLSERVER_PASSWORD`, `AZURE_STORAGE_CONNECTION_STRING`, `ANTHROPIC_API_KEY`), not a new risk class.
  - Verification: 19 new unit tests (`credentials.test.ts`, `sessionStore.test.ts` — including injectable-clock boundary tests for sliding expiry, `cookies.test.ts`), each happy/failure/boundary. `mcp-server` 137/137 passing (up from 118), `guardrails` 50/50 unaffected, `tsc --noEmit` clean in both. Live-verified extensively against the real running server rather than trusting the wiring: wrong password → real `401`; correct login → real `Set-Cookie` (`HttpOnly`, `SameSite=Strict`, `Max-Age=3600`, no `Secure` locally as configured); every previously-open route confirmed `401`s without a session and works with one; the full guardrail propose→decide flow confirmed the `approver` field and the audit trail's `actor` field both now show `"quincy"` (the real login) instead of a hardcoded string; confirmed in a real browser via the actual login form that an unauthenticated visit to `/` redirects to `/login`, a successful login lands on the dashboard with the real username/initials shown, `document.cookie` cannot read the session token (`HttpOnly` genuinely blocks JS access — the concrete reason session cookies beat a JWT in `localStorage`), and logout genuinely destroys the server-side session (a subsequent visit to `/` redirects to `/login` again, not just a client-side navigation). Then ran `demo-start` and `demo-stop` for real, end to end, from a cold state — SQL Server itself is genuinely unavailable this month (Azure free-tier monthly quota exhausted, confirmed via the real error message, renews 2026-09-01 UTC — unrelated to this work, reported plainly rather than worked around, per the skill's own "don't claim readiness if a step didn't actually succeed" rule), but every auth-dependent step (server start, login, authenticated monitoring start/stop) was independently verified working, which is what this task needed to prove.
  - Notes: No `.colaberry/plan.json` requirement covers this, so no `Story:` trailer, matching every other governance/tooling entry this session. Explicitly not built (flagged, not silently left worse): `frontend/`'s React/Vite dev-proxy cookie/CORS handling (its own code already admits its deployment topology is undecided — gating `/api/dashboard/summary` means its one existing call will now `401` until that's addressed); real MFA (`mfa: true` stays an honest stand-in — no MFA exists anywhere in this system, only real login identity was added); multi-user credential storage (single `AUTH_USERNAME`/`AUTH_PASSWORD_HASH` pair, matching this repo's actual single-operator reality); login rate-limiting/lockout; the pre-existing unbounded `readJsonBody` size limit (separately flagged earlier, unrelated task). Local `.env` populated with a freshly generated random password for real end-to-end testing; the plaintext value is known to the user via this session's chat history.

- [x] Fix the frontend's now-401ing `/api/dashboard/summary` call (follow-up to session auth, no platform story)
  - Date: 2026-08-23
  - Session: CC-20260819-x3mq
  - What changed: The auth entry immediately above explicitly flagged, rather than silently broke, the separate `frontend/` React app's one API call — user asked to actually address it this same session. `App.tsx`'s existing error handling already turned a `401` into a readable message (`"Sign in required."`, via the existing generic error path), so this wasn't a crash or a blank page — but there was no way to actually *do* anything about it from that screen. Read `frontend/vite.config.ts` and `App.tsx` directly rather than reasoning from the earlier session's summary alone. Confirmed: dev-only Vite proxy (`/api` → `http://localhost:8787`), single `fetch('/api/dashboard/summary?role=...')` call, no router, no login concept, and the file's own header comment already honestly flags "where the built frontend actually reaches the backend in a real deployment is an open question this walking skeleton doesn't answer yet" — this fix works within that existing honesty rather than pretending to resolve production deployment, which stays unresolved and out of scope.
  - What changed (`App.tsx`): added a distinct `'unauthenticated'` `FetchState`, detected on `res.status === 401` before the generic error branch, rendering a real `<a href="/login">Sign in</a>` link instead of lumping it into the generic "Couldn't load the dashboard" message.
  - What changed (`vite.config.ts`): added `/login` to the dev proxy alongside the existing `/api` entry. This is the actual mechanism that makes the fix work, not just the UI affordance: Vite's proxy is server-side, so a browser at `http://localhost:5173` that's proxied through to `http://localhost:8787/login` receives the `Set-Cookie` response as if it came from `:5173` itself — the session cookie ends up scoped to the origin the frontend's own `/api/*` fetches actually run from. Without this, clicking "Sign in" would take the user to a different origin (`:8787` directly) whose cookie this app could never see, and the fix would look complete while still not working.
  - Verification: 1 new test (`App.test.tsx`) confirming a mocked `401` renders `data-testid="dashboard-unauthenticated"` with a real `<a href="/login">` — happy/failure/boundary coverage for the other paths was already in place and left untouched. `frontend` 6/6 tests passing (up from 5), `tsc -b` clean. Live-verified end to end against the real running `mcp-server` and a real `npm run dev` Vite server (both started via plain `Bash`, not the `preview_start` tool — the same `EPERM: process.cwd` sandbox bug from earlier this session affected `mcp-server-http`'s `launch.json` entry again; drove the Browser pane via a plain tab instead, matching the established workaround): visited the React app with no session → correctly showed "Sign in required" with a working link; clicked through → landed on `/login` still at `localhost:5173` (confirming same-origin, not a cross-origin redirect); logged in with the real credentials → real `POST /api/login` returned `200`, landed back on `/` (correctly showing the pre-existing, unrelated "No role specified" state, since `login.html` always redirects to `/` with no query string — not a regression); navigated back to `?role=it-manager` → the dashboard now loaded with real data (3 active incidents, 1 blocked session, honestly labeled "Fixture fallback" since Azure SQL is still paused this month) — the exact call that was `401`ing now succeeds because the session cookie is present and correctly same-origin.
  - Notes: No `.colaberry/plan.json` requirement covers this, so no `Story:` trailer. Still explicitly not solved (unchanged from the entry above): the built/production deployment topology for `frontend/` — this fix only closes the dev-server gap, which is the only environment this app currently runs in. The first click on the login page's submit button silently did nothing (no request fired, no visible error) — a real, reproducible one-off in this session's browser automation tooling, not a bug in `login.html` itself (a second, otherwise-identical click on the same button fired the request correctly); not investigated further since it didn't reproduce on retry and isn't part of the shipped code path.

- [x] Resolve the frontend's production deployment question: serve the built React console from mcp-server itself (no platform story)
  - Date: 2026-08-23
  - Session: CC-20260819-x3mq
  - What changed: `frontend/`'s own code had flagged, since it was written, that "where the built frontend actually reaches the backend in a real deployment is an open question this walking skeleton doesn't answer yet." User asked to resolve it. Entered Plan Mode again (multi-file, a real architectural decision on URL scheme and caching strategy) — had a Plan subagent design it, verified its file/line citations directly against the real `httpServer.ts` before finalizing. Decision: serve the built app from `mcp-server` itself, the same way `dashboard.html` already is — one origin, one process, no CORS, cookies just work, closing the exact gap the previous entry's dev-proxy fix deliberately left open.
  - What changed (URL scheme — the actual design decision): page shell at `GET /console`, built JS/CSS at Vite's own default root-relative `/assets/*`, **no `base` override added to `frontend/vite.config.ts`**. Vite's unmodified default build already emits `dist/index.html` referencing `/assets/<name>-<hash>.js` as an absolute path, which resolves against whatever origin serves the page — confirmed for real by actually running `npm run build-console` and inspecting the output (`dist/assets/index-DnKrWoVO.js`, `dist/assets/index-DXYdht-R.css`, both referenced as `/assets/...` in `dist/index.html`), not assumed from memory of Vite's documented defaults. Rejected the alternative (`base: '/console/'`) specifically because it would create a fragile coupling — a `frontend/vite.config.ts` line that only makes sense in light of a decision made in a completely different file, easy to silently break if that config is touched later by someone with no visibility into `httpServer.ts`'s routing.
  - What changed (new module, `mcp-server/src/staticFiles.ts`): `resolveStaticFilePath(rootDir, requestedPath)` — real path-traversal protection (`candidate === root || candidate.startsWith(root + sep)`, not a bare `startsWith(root)`, which is the classic bug that wrongly accepts a sibling directory like `/tmp/dist-evil` against root `/tmp/dist`), plus an `.isFile()` check rejecting directory requests; returns `null` for anything unsafe or missing, never throws. `mimeTypeFor(filePath)` — a small fixed extension→MIME map with a safe `application/octet-stream` default for anything unmapped. Both pure, no I/O side effects beyond a stat check, following this session's established "thin route, tested logic elsewhere" split (`guardrails/hitlQueue.ts`, `mcp-server/src/auth/sessionStore.ts`).
  - What changed (`httpServer.ts`): two new routes added right after the existing `GET /login` block. `GET /console` checks `existsSync(frontend/dist/index.html)` **per request**, not cached at module load like `dashboardHtml`/`loginHtml` (those are committed source guaranteed to exist; `dist/` only exists if and after a separate build ran, and must never crash the server or affect `/`, `/login`, `/health`, or the API if it hasn't) — missing returns `503 CONSOLE_NOT_BUILT` with a clear message via the existing `sendJson()` pattern (`503`, not `404`, since the route genuinely exists and will work once built). `GET /assets/*` calls `resolveStaticFilePath()`/`mimeTypeFor()` and serves the bytes, or `404`s. Neither route is gated by `requireSession()` — same reasoning already established for `GET /`: the shell and compiled JS/CSS embed no secrets, and `App.tsx`'s existing, already-tested `'unauthenticated'` state (built in the entry above) only works at all if the shell loads *before* auth succeeds. One import line added (`existsSync` alongside the existing `readFileSync`).
  - What changed (build convenience): one new script in `mcp-server/package.json` — `"build-console": "npm run build --prefix ../frontend"`, optional, never runs automatically (this repo has zero deploy tooling and adding a Dockerfile/CI pipeline was explicitly out of scope for this task). Documented as a third way to see the app in `README.md`'s "See it running" section, alongside the existing `npm run http` and `npm run dev` instructions.
  - Verification: 17 new unit tests (`staticFiles.test.ts`) — happy path, missing file, traversal, the sibling-directory-string-prefix boundary bug specifically, root-itself and sub-directory boundaries, every mapped MIME extension plus unmapped/no-extension cases. `mcp-server` 154/154 passing (up from 137), `tsc --noEmit` clean. Live-verified extensively, matching this session's standard for anything touching `httpServer.ts` (which has no dedicated test file and is verified live throughout): actually ran `npm run build-console` for real and inspected the real output before trusting any asset-path assumption; started the server with `frontend/dist/` temporarily renamed away and confirmed a real `503 CONSOLE_NOT_BUILT` while `/`, `/login`, and `/health` stayed unaffected; restored `dist/`, confirmed `GET /console` returns `200` with the real built HTML and `GET /assets/*.js`/`.css` return `200` with correct content types; opened `/console` in a real browser tab and confirmed zero console errors (all assets loaded); confirmed the app's existing `'unauthenticated'` state renders correctly with no session, then logged in for real and confirmed `/console?role=it-manager` loads genuine data (3 active incidents, 1 blocked session, honest "Fixture fallback" label) — the same call that was previously broken, now working with zero proxy involved at all, unlike the dev-mode fix in the entry above. Live-tested the traversal guard directly against the running server too (`.../assets/../../../../etc/passwd` and a `%2f`-encoded variant), both correctly `404`, on top of the unit tests covering the same logic in isolation.
  - Notes: No `.colaberry/plan.json` requirement covers this, so no `Story:` trailer. Explicitly not built, matching the plan's own stated non-goals: any Docker/CI/deploy pipeline (zero deploy tooling exists in this repo today; that's a separate, much larger decision); changes to `frontend/`'s own source; a `/console/*` SPA-routing catch-all (nothing in `frontend/` does client-side routing yet — flagged as a one-line future extension, not needed now); the pre-existing, unrelated gap where `mcp-server`'s own `tsc build` doesn't copy `.html` files into `dist/` (real, confirmed by the Plan subagent's research, but unrelated to this task). Both dev servers (`mcp-server`, Vite) stopped at the end of this session's work, per the user's standing preference stated earlier this session.

- [x] Architect Expo package: refreshed architecture materials, committed to the repo (no platform story)
  - Date: 2026-08-24
  - Session: CC-20260819-x3mq
  - What changed: User asked to publish a shareable architecture package for the Architect Expo. Checked `project-blueprint/` first rather than building from scratch — it already held a full package (`index.html`, numbered `01-idea.html` through `07-not-covered.html`, `one-pager.pdf`, `reviewer-one-pager.md`), but dated Aug 6–17 and describing an early walking skeleton (its own text: "30/30 tests... live SQL Server path has never actually run"), predating the guardrail approval flow, session auth, and audit trail built since. Flagged this to the user rather than republishing stale content; they chose to refresh first. Verified every number used below by actually running the test suites and grepping real per-file test counts — several of a first-pass estimate were wrong by a wide margin (e.g. the audit-trail row was undercounted by 9), caught and corrected before anything was written down or published.
  - What changed (three Claude Artifacts, built across several requests this session, now all saved as real files): `project-blueprint/expo/architecture-summary.html` — the main page (problem, the detection-to-decision flow, a real captured audit-trail example, the guardrail's principle, a 7-layer implementation-mapping table, an honest built/gap status table, the stack); `project-blueprint/expo/layer-diagram.html` — the 7-layer architecture as an SVG diagram, showing the governance gate between reasoning and production write-back explicitly (the one arrow that's absent is the point), plus a correlation-ID side-panel tapping every layer; `project-blueprint/expo/trust-boundaries.html` — a left-to-right data-flow diagram marking the four real points where CoreOps stops trusting what it's holding (identity, external services, rendering, the action gate), each tied to the actual enforcing code. `project-blueprint/expo/architecture-summary.pdf` — a reportlab-generated print version of the main page, same verified numbers. `project-blueprint/expo/README.md` documents the relationship between these files and their live Claude Artifact URLs (all three still hosted on claude.ai, private by default — this repo only holds the editable source).
  - Verification: Each diagram was rendered to a raster image (extracting the `<svg>`, resolving CSS custom properties to literal colors, rendering via PyMuPDF) and visually inspected before publishing — this caught one real, reproducible bug: the layer diagram's Governance-to-Orchestration gap was 158px while every other inter-box gap was ~52-56px, a coordinate math error that would have rendered as an obviously broken layout. Fixed by recomputing all box y-coordinates for a consistent 56px gap and re-rendering to confirm. The trust-boundary diagram was planned coordinate-by-coordinate before writing any SVG (learning from that bug) and rendered clean on the first pass. The PDF was rendered page-by-page to PNG and visually checked the same way earlier this session, catching a real page-break bug (an orphaned two-line tail on an otherwise-blank final page) fixed via `KeepTogether`. All three Artifact URLs confirmed live and reachable.
  - Notes: No `.colaberry/plan.json` requirement covers this, so no `Story:` trailer. Named the local copy `project-blueprint/expo/` rather than extending the old numbered sequence (`01-idea.html` etc.) — those files describe a different, earlier state of the system and are left untouched rather than silently overwritten; the README in `expo/` explains the supersession honestly instead of deleting the old material. These HTML files are built for Claude's Artifact publishing specifically (self-contained, Google Fonts, no other external assets) — they are not wired into `mcp-server`'s or `frontend`'s build or serving in any way, and aren't meant to be.
