[project]
name = "coreops-dependency-map"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Quincy Ninying", email = "qninying@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=2.1.0",
]

[tool.uv]
package = false
